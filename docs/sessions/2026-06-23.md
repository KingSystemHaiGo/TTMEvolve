# 2026-06-23 Session Notes

## LLM Runtime Interview Follow-Up

User asked whether the now-connected LLM can be interviewed directly so it can report what feels awkward in real agent use, then have Codex iterate on those problems.

Previous runtime interview with the configured MiniMax-M3 path identified the top pain point:
- Tool validation failures were too opaque for the model.
- ReAct only fed back prose errors, so the model could repeat the same invalid tool call.
- Recommended observation shape: stable machine-readable fields such as `valid:false`, `rule_id`, `reason`, and `suggested_fix`.

Delivered:
- Replaced `agent/tool_validator.py` with an ASCII, deterministic lightweight JSON-schema validator.
- Kept legacy `errors` for compatibility and added `structured_errors` for LLM repair.
- Added structured rules for missing required params, wrong types, unknown params, enum mismatch, min/max items, string length, and numeric range.
- Updated unknown-tool validation in `agent/tool_registry.py` to return `rule_id=unknown_tool`, a repair hint, and a capped available-tools list.
- Updated `agent/react_loop.py` so invalid calls produce an observation with:
  - `valid: false`
  - `failure_type: tool_validation`
  - `rule_id`
  - `path`
  - `reason`
  - `suggested_fix`
  - `structured_errors`
- ReAct now injects a compact JSON `[tool_validation_failed]` context hint before the next LLM turn.
- Added regression coverage in `tests/test_tool_call_validation.py`.

Verification:
- Bundled Python `py_compile` passed for `agent/tool_validator.py`, `agent/tool_registry.py`, `agent/react_loop.py`, and `tests/test_tool_call_validation.py`.
- Direct function-run coverage passed for every `tests/test_tool_call_validation.py` test.

Next:
- Run another LLM interview after this change and compare whether repeated invalid tool calls decrease.
- Then address the next interview findings: BrowserView/Playwright authority split, cancel/abort support, dry-run/diff for risky actions, Maker MCP diagnostics, and client focus context.

## Cooperative Session Cancellation

The follow-up LLM runtime interview was attempted after structured validation, but the call timed out after 120 seconds. Treating that as another runtime friction signal, the next concrete improvement was cancellation/abort support so long or stuck runs can be stopped without closing the whole GUI.

Delivered:
- Added `core/cancellation.py` with `TaskCancelled`.
- Added cooperative `cancel_check` support to `TapMakerAgent` and `ReActLoop`.
- ReAct now checks cancellation at iteration boundaries, after LLM calls, and before/after tool execution.
- Agent post-processing checks cancellation before trajectory collection, repair, and learning work.
- Added `Session.cancelled` and `pending_action_id` in `server/app_server.py`.
- Added `POST /sessions/{id}/cancel`.
- Cancelling a pending approval automatically responds `False` to unblock the worker.
- Canceled sessions are persisted as `status=canceled` via `SessionStore.mark_cancelled()`.
- Session status/details now return `canceled: true` for canceled terminal sessions.
- Frontend `useBackend()` exposes `cancelCurrent()`.
- `ChatInput` now shows a running-state stop button while still allowing new typed messages to queue.

Verification:
- Bundled Python `py_compile` passed for the changed backend files and tests.
- `tests/test_app_server_can_cancel_running_session()` passed.
- Full direct function-run coverage passed for `tests/test_app_server_resume.py`.
- Full direct function-run coverage passed for `tests/test_session_store.py`.
- `npm.cmd --prefix frontend run build` passed.

Known boundary:
- Cancellation is cooperative. If a single external API/tool call is already blocking, it stops at the next cancellation checkpoint. Future work should add provider/tool-level request timeouts and MCP/browser call cancellation where supported.

Next:
- Add runtime latency telemetry for first response, LLM phases, tool calls, and task gaps.
- Re-run the LLM interview with an explicit timeout/retry wrapper and record model feedback as a structured artifact.

## Runtime Latency Telemetry

Delivered:
- Added ReAct latency SSE events for:
  - `first_response`
  - `llm_think`
  - `llm_action`
  - `iteration_planning`
  - `tool_call`
  - `session_total`
- Latency payloads include `phase`, `elapsed_ms`, and contextual fields such as `iteration`, `tool`, and `ok` where relevant.
- Frontend `AgentWorkbenchState` now tracks latency summary fields:
  - first response
  - latest LLM phase
  - latest tool call
  - total session time
- Agent Workbench displays a compact latency row without adding debug noise to the main chat transcript.
- Tool run rows can show per-tool elapsed time once the matching latency event arrives.
- Added a regression test proving ReAct emits all core latency phases during a simple tool-using run.

Verification:
- Bundled Python `py_compile` passed for `agent/react_loop.py` and updated tests.
- Full direct function-run coverage passed for `tests/test_tool_call_validation.py`.
- Full direct function-run coverage passed for `tests/test_app_server_resume.py`.
- Full direct function-run coverage passed for `tests/test_session_store.py`.
- `npm.cmd --prefix frontend run build` passed.

Next:
- Add timeout wrappers around LLM self-interview and provider calls so slow calls fail clearly instead of hanging the improvement loop.
- Promote Maker MCP diagnostics and per-MCP-call latency into the same Workbench runtime surface.

## API Timeout Hardening

Delivered:
- Added `llm/api_errors.py` with shared `LLMAPIError` and `LLMTimeoutError`.
- OpenAI-compatible, MiniMax, and Claude clients now default to `llm.timeout=45` seconds instead of 120.
- Provider clients now convert timeout/network failures into structured runtime exceptions.
- Failed API calls update `last_call_stats()` with `error_type`, `error`, `generate_ms`, and zero token counts, so the existing `llm_usage` event path can still expose the failure.
- `scripts/llm_runtime_interview.py` now clones config, uses `llm.interview_timeout` defaulting to 30 seconds, and returns structured JSON for `llm_interview_timeout` / `llm_interview_api_error`.
- AppServer now emits a final `llm_usage` event when a fatal LLM exception includes last-call stats.
- `config.example.json` documents `llm.timeout` and `llm.interview_timeout`.

Verification:
- Running `scripts/llm_runtime_interview.py` against the current MiniMax config now returns structured timeout JSON after about 30 seconds instead of hanging until the outer shell timeout.
- Bundled Python `py_compile` passed for changed LLM clients, AppServer, script, and tests.
- `tests/test_api_llm_errors.py` passed.
- Full direct function-run coverage passed for `tests/test_app_server_resume.py`.
- Full direct function-run coverage passed for `tests/test_tool_call_validation.py`.
- `npm.cmd --prefix frontend run build` passed.

Next:
- Add Maker MCP status, last-call, timeout, and retry diagnostics.
- Feed API timeout/error stats into a visible Workbench runtime health strip.

## Maker MCP Diagnostics Surface

Delivered:
- Added Maker MCP diagnostic state to `agent/mcp_integration.py`.
- MCP integration now records:
  - connected timestamp
  - last connection error
  - registered tool count and tool schemas
  - last MCP call tool name
  - last MCP call success/failure
  - last MCP call elapsed time
  - last MCP call parameter keys
- Added `MCPIntegration.status()`.
- Added AppServer endpoints:
  - `GET /mcp/status`
  - `GET /mcp/tools`
- Endpoints return a disconnected status instead of failing when Maker MCP integration is unavailable.
- Frontend now polls `/mcp/status`.
- Cockpit header Maker MCP pill now shows connected/disconnected state and tool count instead of only package version.
- Maker MCP tooltip includes package version, last call summary, and last error.

Verification:
- Bundled Python `py_compile` passed for `agent/mcp_integration.py`, `server/app_server.py`, and `tests/test_mcp_diagnostics.py`.
- `tests/test_mcp_diagnostics.py` passed using the fake MCP server.
- `tests/test_mcp_client.py` passed.
- Full direct function-run coverage passed for `tests/test_app_server_resume.py`.
- Full direct function-run coverage passed for `tests/test_tool_call_validation.py`.
- `npm.cmd --prefix frontend run build` passed.

Next:
- Add timeout/retry around individual MCP stdio requests.
- Emit MCP call latency into the same `latency` / Workbench runtime surface used by LLM/tool telemetry.

## Three-Layer Communication Protocol

User asked to continue and verify whether the implementation really matches the self-evolving agent design docs, especially communication between the three layers.

Reviewed:
- D:/CC/Ima导出/自进化agent之一.md through 自进化agent之七.md.
- Key requirement: the three layers must form a governed feedback system, not three decorative labels. Agent proposes/acts through a narrow gateway, runtime records/diagnoses/repairs, learning converts trajectories into versioned knowledge without mutating live execution.

Delivered:
- Added core/layer_events.py with a typed LayerEvent contract.
- TapMakerAgent now emits structured layer handoff events with schema_version, event, source_layer, 	arget_layer, correlation_id, cause, metrics, and 	imestamp.
- Current flow is visible as user -> agent, gent -> runtime, 
untime -> learning, and learning -> storage.
- Frontend AgentWorkbenchState now stores the protocol fields.
- AgentWorkbench displays route, event name, and selected metrics for each layer rather than static architecture copy.
- Workbench layer styling now allows wrapped content instead of truncating the actual communication details.
- Added 	ests/test_layer_events.py.
- Added architecture compliance report: docs/architecture/self-evolving-agent-compliance-2026-06-23.md.

Verification:
- Bundled Python py_compile passed for core/layer_events.py, gent/agent.py, and 	ests/test_layer_events.py.
- Direct function-run coverage passed for 	ests/test_layer_events.py.
- Direct function-run coverage passed for all 	ests/test_tool_call_validation.py tests.
- Direct smoke for 	ests/test_app_server_resume.py::test_app_server_session_persisted_and_listed passed.
- 
pm.cmd --prefix frontend run build passed.
- pytest is not installed in the bundled Python, so tests were run by direct function invocation.

Next:
- Mirror selected repair/evolution EventLog events into layer SSE so runtime repair is visible in the Workbench.
- Convert learning into an async proposal queue with validation and rollback.
- Add tamper-evident event ids/hash chain for authority replay.

## LLM-as-User Feedback Loop

User emphasized that the connected LLM is the main user of the Agent runtime and should participate in ongoing optimization.

Observed from actual LLM interview attempts:
- Full interview against current MiniMax-M3 timed out at 30s.
- New light interview also timed out at 12s and again at 30s, even with lower max_tokens.
- The direct feedback artifact now records this as llm_interview_timeout, with call stats showing zero generated tokens and the read timeout.

Delivered:
- Rebuilt scripts/llm_runtime_interview.py as a light/default feedback collector.
- Added --mode light|full, --timeout, --max-tokens, --provider, --model, --base-url, and --save.
- Added llm.feedback.{provider,model,base_url,timeout} config support so feedback/interview can route to a faster model independently of the main Agent model.
- Added durable JSON feedback artifacts under docs/llm-feedback/.
- Added scripts/summarize_llm_feedback.py to aggregate feedback runs and classify failures.
- Hardened feedback parsing so non-object JSON is recorded as invalid_feedback_shape instead of crashing.
- Documented feedback config in config.example.json and config.embedded.json.
- Added 	ests/test_llm_runtime_interview.py.

Verification:
- py_compile passed for the feedback scripts and tests.
- Direct function-run tests passed for 	ests/test_llm_runtime_interview.py.
- scripts/summarize_llm_feedback.py reports current counts: 2 llm_interview_timeout, 1 invalid_feedback_shape.

Next:
- Configure a faster feedback model/provider through llm.feedback or CLI override and rerun scripts/llm_runtime_interview.py --mode light --save.
- Once a successful feedback JSON arrives, implement the top smallest_fix and repeat the loop.

## Runtime Communication Envelope

User asked to optimize project-internal communication and continue the LLM feedback loop.

Delivered:
- Added core/runtime_events.py with a shared RuntimeEvent envelope.
- Runtime events now carry meta.schema_version, event_id, channel, source, correlation_id, parent_id, and 	imestamp while preserving the existing SSE 	ype/session_id/payload shape.
- server.app_server.Session.emit() now envelopes every live SSE event before queueing and persistence.
- SessionStore now stores optional source and meta_json columns with backward-compatible migration.
- get_events() returns source and meta for replayed events.
- LLM feedback artifacts now include a standard llm_feedback runtime event with channel=feedback.
- scripts/summarize_llm_feedback.py exposes latest event meta and still aggregates timeout/invalid-shape counts.
- Added 	ests/test_runtime_events.py; expanded 	ests/test_session_store.py.

Verification:
- py_compile passed for runtime event, AppServer, SessionStore, feedback scripts, and tests.
- Direct function-run tests passed for runtime events, session store, and LLM feedback scripts.
- AppServer smoke confirmed persisted events now include meta with event_id/channel/source/correlation_id.
- Mock LLM feedback save confirmed artifact includes event.meta; summary reports 2 llm_interview_timeout and 2 invalid_feedback_shape.

Next:
- Add a small frontend/debug surface for event meta and LLM feedback summary.
- Bridge repair/evolution EventLog entries into the same runtime event envelope.
- Configure a faster real llm.feedback provider and continue the LLM-as-user loop.

## MiniMax Call Verification And Tool Timeout Feedback

User suspected the system might not be calling the MiniMax API at all.

Observed:
- The configured MiniMax key path does make real HTTP requests to MiniMax.
- The legacy `text/chatcompletion_v2` endpoint returned HTTP 200 with MiniMax-M3 and valid content on a tiny probe.
- The slow LLM feedback runs were caused by service-side reasoning/generation latency, not by the API path being unused.
- A successful light feedback artifact identified the current P0: blocking tool calls can stall the whole ReAct loop because cancellation only runs before/after tool execution.

Delivered:
- `Executor` now has configurable `runtime.tool_timeout_seconds` and `runtime.shell_timeout_seconds`.
- `execute_shell` now uses `Popen.communicate(timeout=...)`; on timeout it kills the Windows process tree, returns quickly, and emits a structured partial observation with `error_type=tool_timeout`, `partial=true`, `elapsed_ms`, stdout/stderr best-effort tails, and `timeout_seconds`.
- `MakerMCPClient` now supports per-request stdio timeouts using a reader thread + queue, which works on Windows pipes where `select` is unavailable.
- MCP timeout tears down the stale MCP process to avoid late stdout corrupting future request/response matching.
- `MCPIntegration` passes `maker_mcp.request_timeout_seconds`, records timeout diagnostics in `last_call`, and marks the client disconnected after timeout.
- ReAct now injects a compact `[tool_timeout]` JSON context hint so the LLM can continue reasoning from partial output instead of retrying the same blocked call blindly.
- `execute_shell` schema now accepts optional `timeout_seconds`.
- Config examples document `runtime.tool_timeout_seconds`, `runtime.shell_timeout_seconds`, and `maker_mcp.request_timeout_seconds`.
- Added `tests/test_tool_timeouts.py`; fake MCP fixture now includes a slow tool.

Verification:
- Bundled Python `py_compile` passed for changed backend files and tests.
- `tests/test_mcp_client.py` passed.
- `tests/test_tool_timeouts.py` passed.
- `tests/test_mcp_diagnostics.py` passed.
- Direct function-run coverage passed for `tests/test_tool_call_validation.py`, `tests/test_session_store.py`, and `tests/test_runtime_events.py`.

Next:
- Re-run the real LLM feedback loop after this timeout patch and see whether the top pain point moves from "blocked by tools" to the next workflow issue.
- Surface tool timeout/partial observations more explicitly in AgentWorkbench, not only in the raw observation payload.

## Commit-State Feedback For Side-Effecting Tools

After the tool timeout patch, a new real MiniMax-M3 light feedback run succeeded in about 13s.

New top pain:
- After API timeout/cancel, the LLM cannot tell whether a write actually committed.
- Retrying may duplicate writes; skipping may drop writes.
- Workbench state and remote real state need a shared observable key.

Delivered:
- `Executor` now adds commit-state fields to side-effecting tool observations:
  - `idempotency_key`
  - `committed`
  - `observed_at`
- Successful write-like tools return `committed=true`.
- Explicit failed write-like tools return `committed=false`.
- Timed-out write-like tools return `committed=null` so the LLM knows the state is uncertain and should verify instead of blindly retrying.
- Side-effect detection covers built-in write tools, Maker asset/generation tools, dynamic write/create/delete/update/edit/save/publish/upload-style tools, and shell commands that look write-like.
- `AgentWorkbenchState` now stores the latest commit-state signal.
- `AgentWorkbench` displays the latest idempotency key as:
  - confirmed write
  - not committed
  - pending confirmation
- Added regression coverage for write commit-state in `tests/test_tool_timeouts.py`.

Verification:
- Real LLM feedback run saved `docs/llm-feedback/llm-runtime-interview-20260623-032225.json`.
- Bundled Python `py_compile` passed for backend changes.
- `tests/test_tool_timeouts.py` passed.
- `tests/test_mcp_client.py` passed.
- `tests/test_mcp_diagnostics.py` passed.
- Direct function-run coverage passed for `tests/test_tool_call_validation.py`, `tests/test_session_store.py`, and `tests/test_runtime_events.py`.
- `npm.cmd --prefix frontend run build` passed.

Next:
- Add a query/reconcile action for `committed=null` cases, especially Maker MCP writes where the remote tool can expose task/file ids.
- Persist commit-state history beyond the in-memory Workbench latest value.

## LLM Feedback Critique And Commit Reconciliation

User clarified the objective: keep talking with the LLM to find flaws in this native AI tool, but do not merely accept what it says.

Process improvement:
- Updated `scripts/llm_runtime_interview.py` so the feedback prompt reflects current facts:
  - tool/MCP timeout already exists
  - partial observations already exist
  - commit-state fields already exist
  - Workbench already displays latest commit state
- Added a lightweight critique pass to flag obviously stale feedback that repeats already-completed mechanisms.
- New feedback artifact: `docs/llm-feedback/llm-runtime-interview-20260623-033429.json`.
- The new feedback passed critique: no stale completed-mechanism claim detected.
- Important discipline: the LLM suggested fictional file paths such as `orchestrator/post_call_hook.py`; implementation was mapped to real project boundaries instead.

New top pain:
- `committed=null` still leaves the Agent uncertain because no automatic post-call reconciliation exists.

Delivered:
- Added `core/commit_state.py` with `CommitStateStore` and `reconcile_observation()`.
- `Executor` now persists side-effect commit observations to `.ttmevolve/commit_state.jsonl`.
- `Executor.reconcile_commit_state()` performs best-effort reconciliation:
  - local `modify_file` can be verified by file existence
  - local `delete_file` can be verified by file absence
  - remote/Maker effects remain `unknown` until provider-specific task/file ids exist
- `ReActLoop` now detects `observation.committed is None`, emits `commit_reconcile` events, invokes the reconciler, and updates the trajectory observation before the next reasoning step.
- Frontend `useBackend` now consumes `commit_reconcile` events and updates Workbench from "checking" into confirmed/failed/unknown state.
- Added tests for commit-state storage, local reconciliation, and ReAct `commit_reconcile` events.

Verification:
- Bundled Python `py_compile` passed for `core/commit_state.py`, `core/executor.py`, `agent/react_loop.py`, `scripts/llm_runtime_interview.py`, and tests.
- `tests/test_tool_timeouts.py` passed.
- `tests/test_tool_call_validation.py` passed, including the new ReAct reconciliation event test.
- `tests/test_mcp_client.py` passed.
- `tests/test_mcp_diagnostics.py` passed.
- `tests/test_session_store.py` passed.
- `tests/test_runtime_events.py` passed.
- `npm.cmd --prefix frontend run build` passed.

Next:
- Add Maker MCP provider-specific reconciliation using remote task/file ids when available.
- Add an endpoint or debug drawer for commit-state history instead of only latest Workbench state.
- Run another LLM feedback pass after reconciliation so the top pain can advance again.

## Tool Progress Heartbeat

Latest successful MiniMax feedback identified the new P0:
- Long-running tools had timeout/partial observations, but no progressive "still running" signal before completion.
- The Agent could not tell whether a Maker MCP / shell / remote task was advancing, stuck, or safe to wait for.

Delivered:
- `ReActLoop` now runs tool execution through `_execute_action_with_progress()`.
- While a tool is still running, ReAct emits periodic `tool_progress` SSE events with:
  - `iteration`
  - `tool`
  - `elapsed_ms`
  - `heartbeat_count`
  - `partial=true`
- Heartbeats are runtime events only; they do not add noisy messages to the main chat transcript.
- `AgentWorkbenchState` now stores progress fields on the matching running tool row.
- `AgentWorkbench` displays running tool elapsed time and heartbeat count before the final observation arrives.
- Added regression coverage proving `tool_progress` is emitted before `observation` for a slow dynamic tool.

Verification:
- Bundled Python `py_compile` passed for `agent/react_loop.py`, `scripts/llm_runtime_interview.py`, `tests/test_tool_call_validation.py`, and `tests/test_llm_runtime_interview.py`.
- `npm.cmd --prefix frontend run build` passed.
- `tests/test_tool_call_validation.py` passed.
- `tests/test_tool_timeouts.py` passed.
- `tests/test_llm_runtime_interview.py` passed.
- `tests/test_runtime_events.py` passed.
- `tests/test_mcp_client.py` passed.
- `tests/test_mcp_diagnostics.py` passed.

LLM feedback result:
- Re-ran real MiniMax light feedback after the heartbeat patch.
- Latest run saved `docs/llm-feedback/llm-runtime-interview-20260623-041648.json`.
- It timed out after about 25.1s before response headers: `response_started=false`.
- This is evidence that MiniMax is still being called, but the feedback loop itself needs a faster `llm.feedback` provider/model or explicit `--retry-same-provider`.

Next:
- Configure `llm.feedback` to a faster low-latency provider/model so the self-feedback loop does not spend most cycles waiting on MiniMax first response.
- Add a Workbench/debug drawer for feedback artifacts and event metadata.
- Extend progress events with provider-specific Maker MCP subtask ids when Maker tools expose them.

## Feedback Fallback And Remote Identity Diagnostics

User asked to keep optimizing with the LLM as the primary runtime user.

Observed:
- Current MiniMax-M3 feedback path is real but still times out before response headers at an 8s budget (`response_started=false`), so it should not be the only feedback path.
- DeepSeek fallback initially produced reasoning-only empty content at low token budgets; after raising repair retry tokens, it returned complete parseable JSON.
- Successful feedback artifact: `docs/llm-feedback/llm-runtime-interview-20260623-044813.json`.
- Valid top pain: Maker MCP remote task/file id lookup is not yet visible enough. The model suggested external Maker MCP server paths, so implementation was mapped to this repo's real boundary instead.

Delivered:
- `scripts/llm_runtime_interview.py` now has `--repair-max-tokens` and a `_repair_max_tokens()` policy with CLI > fallback spec > config > 1024 default precedence.
- `core/health.py` now classifies `context_saturation` as `normal/elevated/high/critical` and degrades health when `context_window_ratio > 0.9`.
- Runtime layer events now include `context_saturation`; AgentWorkbench displays `token_usage_ratio`, `context_window_ratio`, and `context_saturation` metrics.
- `MCPIntegration.status()` now includes `remote_identity` diagnostics:
  - task lookup tool candidates
  - file lookup tool candidates
  - tools with id-like parameters
  - missing lookup capabilities
  - id fields observed in the latest MCP call result
- Cockpit header Maker MCP pill now includes remote id status and tooltip summary.
- Fake MCP server and diagnostics tests now cover task/file list tools and id-field detection.

Verification:
- `py_compile` passed for changed Python files.
- `tests/test_mcp_diagnostics.py` passed.
- `tests/test_mcp_client.py` passed.
- `tests/test_llm_runtime_interview.py` passed.
- `tests/test_health_monitor.py` passed.
- `tests/test_layer_events.py` passed.
- `npm.cmd --prefix frontend run build` passed.

Next:
- Use `remote_identity.status` to guide Maker MCP-specific reconciliation for `committed=null` remote side effects.
- Add a commit-state history/debug entry so the Agent can query prior idempotency keys and unknown remote effects.
- Continue LLM feedback with fallback routing enabled; treat provider-specific paths from feedback as suggestions, not literal repo targets.

## Remote Commit Reconciliation From LLM Feedback

User's standing objective remains: keep talking with the LLM to find real weaknesses, but do not blindly implement hallucinated paths or stale advice.

Observed:
- After updating the feedback prompt with current facts (`remote_identity` and `context_saturation` already exist), MiniMax still timed out before headers at an 8s budget.
- DeepSeek fallback first returned reasoning-only empty content; the repair retry needs a larger timeout than normal fallback.
- A failed artifact, `docs/llm-feedback/llm-runtime-interview-20260623-045651.json`, exposed a feedback-chain bug: a skipped Qwen fallback with no key could mask the more useful DeepSeek timeout result.
- After fixing repair timeout and skipped-fallback precedence, successful artifact `docs/llm-feedback/llm-runtime-interview-20260623-045829.json` identified the next valid P0: use remote task/file ids to automatically reconcile `committed=null`.
- The LLM again suggested fictional `src/...` paths, so implementation was mapped to real files: `core/commit_state.py`, `core/executor.py`, `agent/mcp_integration.py`, and frontend Workbench state.

Delivered:
- `scripts/llm_runtime_interview.py` now has `--repair-timeout` and `_repair_timeout_seconds()` so reasoning-model repair retries can use a larger budget than the first fallback attempt.
- Feedback fallback selection no longer lets a skipped unconfigured provider replace a real previous failure as the final diagnostic payload.
- `core.commit_state.reconcile_observation()` now accepts an optional remote resolver and preserves explicit remote statuses such as `remote_lookup_no_match`.
- `Executor` now supports `set_remote_commit_resolver()` and passes it into commit reconciliation.
- `MCPIntegration` registers a remote commit resolver when connected or attached to a session agent.
- The remote resolver:
  - uses `remote_identity` task/file lookup candidates;
  - calls up to three matching lookup tools;
  - only sets `committed` when a returned remote record matches explicit observation identity such as path, remote id, task/file id, or idempotency key;
  - returns `verified_remote`, `remote_lookup_no_match`, or `remote_lookup_unavailable` instead of guessing.
- Workbench commit state now preserves and displays `reconcile_status`, `remote_lookup_tool`, and lookup attempt count.
- Added regression tests for successful remote file reconciliation, no-match behavior, and avoiding false positives from tool-name-only matching.

Verification:
- `py_compile` passed for changed backend files and tests.
- `tests/test_mcp_diagnostics.py` passed.
- `tests/test_tool_timeouts.py` passed.
- `tests/test_tool_call_validation.py` passed.
- `tests/test_llm_runtime_interview.py` passed.
- `npm.cmd --prefix frontend run build` passed.

Next:
- Add a commit-state history API/debug surface so the Agent and user can query prior idempotency keys and reconcile outcomes, not only the latest Workbench strip.
- Update the feedback prompt again before the next LLM interview so it knows remote `committed=null` lookup is now implemented conservatively.

## Tool Preflight, Plan Validation, Context Compression, And Commit History

User asked to keep optimizing with the connected LLM as the primary runtime user.

Observed:
- MiniMax-M3 is still called, but consistently times out before response headers at an 8s feedback budget (`response_started=false`).
- DeepSeek fallback returns useful JSON only after a larger reasoning repair retry budget.
- Feedback artifacts saved this pass:
  - `docs/llm-feedback/llm-runtime-interview-20260623-051043.json`
  - `docs/llm-feedback/llm-runtime-interview-20260623-052405.json`
  - `docs/llm-feedback/llm-runtime-interview-20260623-053011.json`
  - `docs/llm-feedback/llm-runtime-interview-20260623-054209.json`
- LLM-suggested source paths were again fictional; valid suggestions were mapped to the real repo boundaries.

Delivered:
- Tool planning guardrail:
  - `ToolRegistry.preflight_action()` now validates a proposed action and attaches ranked alternative tools plus `suggested_next_step`.
  - ReAct emits `tool_preflight` before execution and feeds failed preflight results back into the next LLM turn.
  - AgentWorkbench displays failed preflight and alternatives without polluting the main chat.
- Lightweight plan validation:
  - Added `core/plan_validation.py`.
  - ReAct now emits `plan_validation` after each observation and stores the report in trajectory plus final result summary.
  - Reports include `pass/warn/fail`, expected evidence, actual evidence, issues, and next check.
  - AgentWorkbench displays the latest validation result.
- Active context compression:
  - `BudgetStats` now exposes `compression_applied`, `dropped_parts`, and `truncated_chars`.
  - `ContextBudgetManager.fit_parts()` now reports compression correctly and adds a `[context_compressed ...]` marker when it can fit.
  - Trajectory slicing keeps recent steps plus older important evidence such as failures, idempotency/commit state, and plan validation warnings.
  - `MemoryManager` gives task and tool descriptions highest priority so tight budgets keep the core instruction surface.
  - Runtime layer metrics now expose compression fields to Workbench.
- Commit-state history:
  - `SessionStore.get_commit_history()` extracts commit-state observations and `commit_reconcile` events from persisted SQLite events.
  - Added read-only HTTP endpoints:
    - `/sessions/{id}/commit-history?steps=5`
    - `/sessions/{id}/submissions?steps=5`

Verification:
- Python `py_compile` passed for changed backend/modules/tests.
- Passed direct tests:
  - `tests/test_plan_validation.py`
  - `tests/test_tool_call_validation.py`
  - `tests/test_context_budget.py`
  - `tests/test_memory_manager.py`
  - `tests/test_session_store.py`
  - `tests/test_app_server_resume.py`
  - `tests/test_llm_runtime_interview.py`
  - `tests/test_mcp_diagnostics.py`
  - `tests/test_tool_timeouts.py`
  - `tests/test_health_monitor.py`
- `npm.cmd --prefix frontend run build` passed.

Next:
- The newest valid LLM pain point after these updates is no longer preflight, plan validation, or context compression. It now points toward richer cross-step goal decomposition and acceptance criteria generation, plus a UI/debug surface for feedback artifacts and event metadata.

## Goal Checklist And Cross-Agent Skill Sync

User asked to continue optimizing with the connected LLM and to check whether implementation matches the intended three-layer/self-evolving Agent design.

Observed:
- MiniMax-M3 is still genuinely invoked but times out before response headers at an 8s feedback budget (`response_started=false`).
- DeepSeek fallback plus repair retry produced usable JSON again.
- The newest feedback repeated "cross-Agent skill sync"; after implementation, the feedback artifact correctly flags this as a stale/completed-mechanism claim unless it specifically means a deeper auto-repair/export loop.
- LLM-suggested files were fictional (`agent/skill_sync.py`, `shared/skill_registry.py`, `agent/service.py`), so the work was mapped to the real `ecosystem/*` and AppServer boundaries.

Delivered:
- Cross-step goal checklist:
  - Added `core/goal_tracking.py`.
  - ReAct now emits `goal_checklist` at session start and after each plan validation update.
  - Open criteria are fed back into the next ReAct context, and final results include the checklist.
  - AgentWorkbench displays compact acceptance criteria.
- Cross-Agent skill sync status:
  - Added `ecosystem/skill_sync.py` with `SkillSyncRecord`, skill content fingerprinting, canonical/Hermes/OpenClaw/Claude Code/Codex-local discovery, manifest generation, and conflict detection.
  - Added `scripts/sync_skills.py` to build/write `storage/skill_sync/manifest.json` and optionally fail on conflicts.
  - Added read-only `GET /skills/sync-status` so runtime/UI/agents can query the current sync manifest.
  - Added tests for matching exports, version conflicts, same-version content drift, manifest writes, and the AppServer endpoint.
- Updated `scripts/llm_runtime_interview.py` so future interviews know skill sync manifest/conflict detection exists and stale skill-sync claims are flagged for human review.

Verification:
- Python `py_compile` passed for new/changed skill sync, AppServer, LLM feedback, and test files.
- Passed direct tests:
  - `tests/test_goal_tracking.py`
  - `tests/test_tool_call_validation.py`
  - `tests/test_llm_runtime_interview.py`
  - `tests/test_skill_sync.py`
  - `tests/test_cross_ecosystem.py`
  - `tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build` passed.
- `scripts/sync_skills.py --project-root . --write` wrote `storage/skill_sync/manifest.json` with zero current records/conflicts because the project has no canonical skills under `storage/skills` yet.

Next:
- Valid next step is not "add skill sync" again; it is either an auto-repair/export loop around the manifest, feedback artifact/event metadata UI, or more domain-specific acceptance criteria for Maker tasks.

## Skill Registry, Dynamic Graph, And Query Tool

Continued the LLM-as-user loop instead of stopping at the first skill sync manifest.

Observed:
- MiniMax-M3 remains genuinely called but continues to time out before response headers at an 8s interview budget.
- DeepSeek fallback plus repair retry continues to return usable JSON.
- LLM feedback kept circling the same topic, but with progressively sharper details:
  - first: cross-Agent skill sync;
  - then: shared registry / export loop;
  - then: skill change broadcast after plan validation;
  - then: unified SkillManifest / dynamic skill graph;
  - finally: Agent-callable discovery via `query_skills`.
- Suggested file paths remained fictional Rust/TypeScript paths, so the valid concepts were mapped to the existing Python `ecosystem`, `agent`, `core`, and AppServer surfaces.

Delivered:
- `ecosystem/skill_sync.py`
  - Added safe export plans over canonical skills.
  - Added `SkillSyncRegistry`, writing `storage/skill_sync/registry.json` plus manifest.
  - Registry now scans both project skills and configured storage skills, fixing the mismatch with `SkillGenerator` / `ToolRegistry`.
  - Added dynamic `skill_graph` with providers, versions, input schema, preconditions, conflicts, pending export actions, and callability.
- `agent/react_loop.py`
  - Emits `skill_sync` at session start.
  - Checks registry after `plan_validation`.
  - If the registry signature changes, generated skills are re-discovered before the next action.
  - If conflicts or review-required export actions exist, emits a `skill_compatibility_warning` context hint and `skill_sync` event after plan validation.
- `agent/agent.py`, `core/executor.py`, `core/sandbox.py`
  - Added low-risk Agent-callable `query_skills` tool over the shared dynamic skill graph.
  - `query_skills` can filter by text, ecosystem, callability, and limit.
- `server/app_server.py`
  - `/skills/sync-status` now returns the shared registry payload, including manifest, export plan, and skill graph.
- `scripts/sync_skills.py`
  - Added `--registry`, `--export-plan`, `--apply-exports`, and guarded review behavior.
- `scripts/llm_runtime_interview.py`
  - Prompt and stale-claim detection now list registry, export plans, skill graph, `skill_sync` events, and `query_skills`.

Verification:
- Python `py_compile` passed for changed backend/runtime/test files.
- Passed direct tests:
  - `tests/test_skill_sync.py`
  - `tests/test_tool_call_validation.py`
  - `tests/test_llm_runtime_interview.py`
  - `tests/test_app_server_resume.py`
  - `tests/test_cross_ecosystem.py`
- `npm.cmd --prefix frontend run build` passed.
- `scripts/sync_skills.py --project-root . --registry` successfully wrote current registry state. Current real project has zero canonical skill records, so the graph is empty but structurally valid.

Next:
- If LLM still says generic "cross-Agent skill sync missing", treat it as stale unless it specifically asks for remote cross-process invocation, MCP-level external agent broadcasting, or UI visualization of the skill graph.
- The next useful improvement is likely either remote invocation semantics for discovered skills or surfacing `skill_sync` / `skill_graph` in AgentWorkbench.

## Workbench Skill Graph UI And Context Sync

Continued the LLM-as-primary-user loop and implemented the next valid runtime coordination step.

Delivered:
- AgentWorkbench now shows Skill Graph sync state, skill/action/conflict counts, and preview rows for conflicts/export actions.
- ReAct now emits compact `context_sync` events:
  - session start emits the initial snapshot;
  - meaningful observation/plan-validation/output changes emit a revisioned snapshot;
  - unchanged snapshots are deduplicated by a stable signature.
- `context_sync` snapshot includes session/task, iteration, trajectory step count, last tool/action param keys, plan verdict, goal checklist state, commit summary, skill sync summary, and compact artifact refs.
- Artifact refs now merge action params with observations, so read/write tools can expose paths even when the observation itself only contains content.
- AgentWorkbench consumes `context_sync` into Runtime layer metrics and displays revision, signature, last tool, plan/goal state, artifact count, and diff keys.
- Removed fixed-height hidden overflow from Workbench thoughts so agent text is not clipped inside a card.
- Updated `scripts/llm_runtime_interview.py` prompt facts and stale-claim detection to treat generic missing `context_sync` feedback as stale.

Verification:
- `python -m py_compile agent/react_loop.py scripts/llm_runtime_interview.py tests/test_tool_call_validation.py`
- `python tests/test_tool_call_validation.py`
- `python tests/test_skill_sync.py`
- `python tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`

LLM feedback:
- MiniMax-M3 is still genuinely called but timed out before response headers at 8s (`response_started=false`).
- DeepSeek fallback+repair produced JSON, but critique flagged `skill_sync_manifest` as stale and suggested fictional Rust paths (`src/agents/skill_registry.rs`, `src/core/tool_preflight.rs`, `src/events/skill_sync.rs`).
- Do not implement that literally. The valid next direction is deeper external-process broadcast/remote invocation semantics on top of the existing registry/context sync, not another local skill registry.

## Context Sync Pull API For External Agents

Continued from the context-sync work because SSE/UI visibility alone is not enough for multi-agent runtime communication.

Delivered:
- Added `SessionStore.get_context_sync_history(session_id, limit)` to extract persisted `context_sync` snapshots from the append-only event log.
- Added read-only HTTP endpoint `GET /sessions/{id}/context-sync?steps=N`.
  - Returns `context_sync`, `latest`, and `count`.
  - Each row includes revision/signature/diff_keys, reason, timestamp, snapshot, last tool, plan verdict, goal state, and artifact count.
  - This gives external agents/processes a pull-based shared-context surface without subscribing to SSE.
- Updated `scripts/llm_runtime_interview.py` prompt facts so future interviews know the pull API exists.

Verification:
- `python -m py_compile server/session_store.py server/app_server.py tests/test_app_server_resume.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_tool_call_validation.py`
- `npm.cmd --prefix frontend run build`

LLM feedback:
- MiniMax-M3 again timed out before response headers at 8s (`response_started=false`).
- DeepSeek fallback again returned generic "cross-Agent skill sync missing" feedback and fictional Go paths (`internal/agent/skill_sync.go`, `internal/registry/skill_registry.go`, `internal/mcp/skill_provider.go`).
- The critique correctly flagged `skill_sync_manifest` as stale. Do not implement another registry unless the feedback specifically targets remote invocation semantics, cross-process broadcast, or distributed context sharing beyond the existing registry/context-sync pull API.

## Updated Goal And Feedback Gate

User updated the persistent goal. The target state is now:
- Any LLM can quickly start coding inside TTMEvolve and work naturally with MakerMCP.
- Agent layer, core runtime layer, and learning/transformation layer operate independently while communicating efficiently.
- Token use is extremely efficient through context compression, tool retrieval, vector memory, and multi-level caching.
- Frontend and backend are fully connected.
- UI should be modern, high-quality, and aligned with the TapTapMaker theme.

Delivered this turn:
- Strengthened `scripts/llm_runtime_interview.py` so valid JSON feedback is not automatically treated as executable.
- Feedback payloads now include `actionable`, `decision`, `actionable_blockers`, and `next_feedback_prompt`.
- Stale feedback that repeats completed mechanisms is marked `actionable=false` with `decision=reject_stale_feedback`.
- Feedback that cites fictional repository paths like `src/` or `internal/` in this Python/Electron repo is marked `actionable=false` with `decision=needs_repo_mapping`.
- Fallback feedback selection now prefers actionable feedback and will not let stale `ok=true` feedback replace actionable feedback.

Verification:
- `python -m py_compile scripts/llm_runtime_interview.py tests/test_llm_runtime_interview.py`
- `python tests/test_llm_runtime_interview.py`

Validation limits:
- Follow-up broader regression hit real disk exhaustion (`WinError 112`) while tests tried to write temp memory index files.
- A direct external LLM feedback run was not executed because escalation review rejected sending TTMEvolve internal architecture/workflow data to DeepSeek/Qwen without explicit user approval.

Next:
- With user approval, run the external LLM feedback script again and confirm stale outputs now produce `actionable=false`.
- Otherwise continue locally by improving MakerMCP onboarding for arbitrary LLMs: expose a concise MakerMCP runtime contract, first-action checklist, and Workbench-visible MCP readiness/usage hints.

## Runtime Contract For Arbitrary LLM Onboarding

Continued locally toward the updated goal without sending internal project data to external LLM providers.

Delivered:
- Added `core/runtime_contract.py`.
  - Builds a compact machine-readable TTMEvolve runtime contract.
  - Summarizes MakerMCP readiness, top Maker tools, remote identity diagnostics, three-layer responsibilities/events, communication endpoints, token-efficiency rules, and skill graph status.
  - Renders a capped prompt block for LLM context injection.
- `TapMakerAgent.runtime_contract(session_id)` now exposes the contract from the live agent state.
- Added low-risk Agent tool `runtime_contract`, so any LLM can query onboarding/runtime rules before choosing tools.
- ReAct initial context now injects `[runtime_contract]...[/runtime_contract]`, giving every provider the same short MakerMCP/TTMEvolve operating contract at task start.
- Added AppServer endpoint `GET /agent/runtime-contract?session_id=...` so frontend and external agents can fetch the same contract.

Verification:
- `python -m py_compile core/runtime_contract.py agent/agent.py agent/react_loop.py server/app_server.py tests/test_runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_llm_runtime_interview.py`

Validation limits:
- Did not run full AppServer/MCP regression in this turn because the environment recently failed with disk exhaustion while writing temporary memory-index files.

Next:
- Surface Runtime Contract in AgentWorkbench/Cockpit so the user can see whether an LLM is operating with MakerMCP-ready guidance.
- Add MakerMCP task templates/checklists for first actions: inspect project, discover Maker tools, choose authority, perform side-effect, verify context/commit evidence.

## Runtime Contract Workbench Surface

Continued the frontend/backend connection and TapTapMaker-themed workbench direction.

Delivered:
- `frontend/src/hooks/useBackend.ts`
  - Added `WorkbenchRuntimeContract` state.
  - After session creation, fetches `GET /agent/runtime-contract?session_id=...`.
  - Stores MakerMCP readiness, tool count, top tools, remote identity, communication endpoints, token-efficiency rules, and warnings.
  - Updates Runtime layer metrics with `contract_tools` and `contract_warnings`.
- `frontend/src/components/AgentWorkbench.tsx`
  - Added Runtime Contract panel.
  - Shows MakerMCP readiness, tool count, remote identity status, token rule count, top Maker tools, context endpoint, and warnings.
  - Keeps the display dense and untruncated rather than fixed-height cards.
- `frontend/src/styles/index.css`
  - Added TapTapMaker-like mint/blue/orange status styling for ready/degraded/offline contract states.

Verification:
- `npm.cmd --prefix frontend run build`
- `python -m py_compile core/runtime_contract.py agent/agent.py agent/react_loop.py server/app_server.py tests/test_runtime_contract.py`
- `python tests/test_runtime_contract.py`

Validation limits:
- Full AppServer/MCP regression was skipped because the environment recently failed with disk exhaustion while writing temporary memory-index files.

Next:
- Add MakerMCP first-action checklist/task templates and wire them into Runtime Contract + Workbench, so a new LLM has an explicit first five actions for Maker coding.

## MakerMCP First-Action Checklist In Runtime Contract

Continued local implementation toward arbitrary LLM onboarding and MakerMCP-first operation.

Delivered:
- Runtime Contract now includes `maker_mcp.first_action_checklist`.
  - Steps: read contract, check MakerMCP, discover authority tools, plan one verifiable Maker change, verify side effects, sync compact context.
  - Connected/tool/remote-identity state marks checklist items as `ready` or `warn`.
- Runtime Contract now includes machine-readable `warning_codes`.
  - Current codes include `maker_mcp_disconnected`, `maker_remote_identity_incomplete`, and `skill_registry_needs_review`.
  - LLM prompt rendering puts warning codes before longer contract content and compacts checklist rows to `id/status/evidence`, so critical state survives truncation.
- Workbench Runtime Contract panel now displays the Maker checklist and warning codes.
  - Frontend maps `warning_codes` to `warningCodes`.
  - Runtime layer metrics include `contract_warning_codes`, `maker_checklist_ready`, and `maker_checklist_warn`.
- Added dense TapTapMaker-like checklist styling.
  - No fixed-height card clipping; checklist rows wrap naturally inside the outer workbench scroll.

Verification:
- `python -m py_compile core/runtime_contract.py tests/test_runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `npm.cmd --prefix frontend run build`

Validation limits:
- Full AppServer/MCP regression still deferred because the environment recently hit disk exhaustion on broader temp-memory-index tests.
- External LLM feedback was not run this turn; it still requires explicit approval before sending internal TTMEvolve architecture/workflow data to third-party providers.

Next:
- Add Maker-specific task templates/acceptance criteria that turn checklist state into concrete "inspect/build/verify/submit" flows.
- Expose Runtime Contract and context-sync pull surfaces to external processes so Claude Code/Codex/opencode-style agents can attach without replaying the full transcript.

## Maker Task Templates And External Agent Attach Contract

Continued the Runtime Contract path from passive onboarding into executable Maker workflows.

Delivered:
- Added `maker_mcp.task_templates` to `core/runtime_contract.py`.
  - Templates: inspect project state, plan one verifiable Maker change, execute and verify side effects, build/submit Maker project, and external agent handoff.
  - Each template includes status, applicable scenario, authority surfaces, steps, acceptance criteria, token strategy, and suggested Maker tools.
  - Template status is derived from MakerMCP connectivity, discovered tools, and remote identity lookup availability.
- Added `external_agents` attach protocol to Runtime Contract.
  - Compatible surfaces include Claude Code, Codex, opencode, OpenClaw, and Hermes-style agents.
  - Attach sequence prefers runtime contract + context sync + MCP status/tools over replaying raw SSE.
- Reworked LLM prompt rendering to stay token-efficient.
  - Communication endpoints are now near the front.
  - LLM prompt version uses compact task template rows and compact top tool rows.
  - UI/runtime contract keeps full human-readable template detail.
- AgentWorkbench now displays Maker task templates and exposes template counts/warnings in Runtime layer metrics.
- Added dense no-fixed-height CSS for template rows, matching the existing non-clipped Workbench direction.

Verification:
- `python -m py_compile core/runtime_contract.py tests/test_runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `npm.cmd --prefix frontend run build`
- Prompt-size smoke: a 30-tool contract rendered under the 900-char cap while preserving key communication endpoints and workflow IDs.

Next:
- Convert template acceptance criteria into live `goal_checklist` seeds for Maker sessions.
- Add an external-agent pull endpoint/documentation bundle that returns runtime contract + latest context sync + skill graph summary in one compact payload.

## Maker Template Acceptance Seeds And Handoff Bundle

Moved Runtime Contract templates from static guidance into live runtime state.

Delivered:
- `core/goal_tracking.py`
  - `derive_goal_checklist()` and `update_goal_checklist()` now accept optional `maker_templates`.
  - Maker template acceptance criteria can seed live checklist items with `source=maker_template` and `template_id`.
  - Applicable templates are selected from the task: Maker inspect/plan for Maker tasks, execute/verify for side-effect tasks, build/submit for build-like tasks, and external handoff for agent handoff tasks.
  - Runtime Contract readiness criteria can be marked done immediately with `runtime_contract` evidence.
  - Plan, side-effect, and build/submit template criteria update from `plan_validation`, `side_effect_state`, and selected tool names.
- `agent/react_loop.py`
  - ReAct now loads the Runtime Contract snapshot at session start.
  - Maker task templates from the contract seed the initial `goal_checklist`.
  - Checklist refreshes preserve the same Maker template criteria across steps.
- `server/app_server.py`
  - Added `GET /agent/handoff?session_id=...&steps=N`.
  - Returns runtime contract, recent context_sync snapshots, latest context, compact skill summary, attach sequence, and a token rule.
- `core/runtime_contract.py`
  - Communication now includes `handoff_bundle`.
  - External agent attach sequence now starts with the handoff bundle.
  - Compact LLM template rendering prioritizes `external_agent_handoff` and omits empty suggested-tool fields.

Verification:
- `python -m py_compile core/goal_tracking.py core/runtime_contract.py agent/react_loop.py server/app_server.py tests/test_goal_tracking.py tests/test_runtime_contract.py tests/test_app_server_resume.py`
- `python tests/test_goal_tracking.py`
- `python tests/test_runtime_contract.py`
- Focused `test_app_server_external_agent_handoff_endpoint`

Validation limits:
- Did not run full app-server regression or frontend build in this slice; touched backend runtime/API and tests only.

Next:
- Surface Maker-template checklist source/template IDs in Workbench so the user can see which Maker workflow is currently driving acceptance.
- Optionally add a GUI/external-agent copy button or endpoint documentation panel for `/agent/handoff`.

## Workbench Maker Template Acceptance Visibility

Made the live Maker acceptance criteria visible in the Agent Workbench.

Delivered:
- `frontend/src/hooks/useBackend.ts`
  - `WorkbenchGoalCriterion` now preserves `source` and `template_id`.
  - `goal_checklist` handling no longer truncates criteria to the first six items, so Maker template criteria are not silently hidden behind generic checklist items.
  - Agent layer metrics now include `maker_goal_templates`.
- `frontend/src/components/AgentWorkbench.tsx`
  - Acceptance panel now counts Maker flow criteria.
  - Maker-template criteria get a dedicated visual treatment and show metadata from `template_id`, evidence, or next check.
  - All criteria render in the Workbench scroll area rather than being clipped to a fixed card size.
- `frontend/src/styles/index.css`
  - Added dense TapTapMaker-like styling for Maker-template acceptance rows.
  - Rows wrap naturally and keep source/evidence text visible.

Verification:
- `npm.cmd --prefix frontend run build`
- `python tests/test_goal_tracking.py`
- `python tests/test_runtime_contract.py`

Next:
- Add a small `/agent/handoff` surface in the UI, probably in Runtime Contract panel: show/copy handoff URL and latest compact attach sequence.

## Workbench External Agent Handoff Surface

Made `/agent/handoff` usable from the Workbench instead of leaving it as a hidden backend endpoint.

Delivered:
- `frontend/src/components/AgentWorkbench.tsx`
  - Runtime Contract panel now shows an `Agent Handoff` block when a handoff endpoint is available.
  - Displays the full local handoff URL, derived from `communication.handoff_bundle`.
  - Shows the first three attach-sequence steps for Claude Code/Codex/opencode-style agents.
  - Adds a `Copy URL` button with copied/error feedback.
- `frontend/src/styles/index.css`
  - Added compact TapTapMaker-like handoff styling.
  - URL and attach steps wrap naturally without fixed-height clipping.

Verification:
- `npm.cmd --prefix frontend run build`
- Focused `test_app_server_external_agent_handoff_endpoint`
- `python tests/test_runtime_contract.py`

Next:
- Consider adding a richer handoff drawer that fetches `/agent/handoff` live and previews the compact bundle, but keep the Runtime Contract panel as the primary fast path.

## Live Handoff Bundle Preview

Turned the Workbench handoff entry from a copy-only URL into a live compact bundle preview.

Delivered:
- `frontend/src/components/AgentWorkbench.tsx`
  - Added a `Preview` / `Refresh` button to the `Agent Handoff` block.
  - Clicking preview fetches the live `/agent/handoff` bundle only on demand, avoiding automatic extra requests.
  - The preview summarizes latest context sync, task, last tool, plan verdict, goal state, artifact count, skill registry state, skill count, conflict count, and token rule.
  - Keeps the full bundle available via the copied endpoint instead of rendering large JSON in the Workbench.
- `frontend/src/styles/index.css`
  - Added compact handoff preview styling.
  - Preview rows wrap naturally and stay within the Workbench flow without fixed-height clipping.

Verification:
- `npm.cmd --prefix frontend run build`
- Focused `test_app_server_external_agent_handoff_endpoint`
- `python tests/test_runtime_contract.py`

Next:
- Shift back toward token/cache/tool retrieval improvements: make ranked tool/context retrieval more observable and cache-aware for arbitrary LLM sessions.

## Cache-Aware Tool And Context Retrieval Metrics

Made the token/tool retrieval path observable without pushing debug noise into the main chat.

Delivered:
- `agent/tool_registry.py`
  - Added a small deterministic ranking cache for `rank_tools()`.
  - Exposes `last_rank_stats()` with candidate count, selected count, ranking latency, cache hit, and cache size.
- `llm/context_budget.py`
  - Tracks token estimation cache hits/misses/cache size.
  - Extends `BudgetStats` with cache and retrieval/build timing fields.
- `memory/manager.py`
  - Adds AGENTS.md hit count, cold recall hit count, and retrieval/build timings to `prepare_think_payload()` stats.
- `agent/react_loop.py`
  - Emits tool-ranking stats on `tool_selection`.
  - Emits a compact `context_budget` event after context fitting.
- `agent/agent.py`
  - Carries cache/retrieval metrics into the final Runtime audit layer event.
- `frontend/src/hooks/useBackend.ts` and `frontend/src/components/AgentWorkbench.tsx`
  - Runtime layer now shows tool candidate/selected counts, ranking cache hit/size, token cache stats, AGENTS/cold recall hits, and context build/retrieval timing.

Verification:
- `python -m py_compile agent/tool_registry.py agent/react_loop.py agent/agent.py memory/manager.py llm/context_budget.py tests/test_tool_call_validation.py`
- `python tests/test_context_budget.py`
- `python tests/test_memory_manager.py`
- `python tests/test_tool_call_validation.py`
- `npm.cmd --prefix frontend run build`

Notes:
- The worktree already contains substantial previous changes; this slice intentionally stayed inside tool/context runtime observability.
- Follow-up cleanup removed the older unreachable mojibake block in `tests/test_tool_call_validation.py`.

Next:
- Use these Runtime layer metrics in a real GUI/API-backed session to see whether the remaining delay is provider latency, context build, ranking, tool execution, or post-run learning/repair.

## Pullable Runtime Metrics API

Made runtime diagnostics available to arbitrary LLMs and external agents without replaying the whole SSE transcript.

Delivered:
- `server/session_store.py`
  - Added `get_runtime_metrics_history(session_id, limit)` over persisted events.
  - Compacts only `latency`, `llm_usage`, `tool_selection`, and `context_budget` events.
- `server/app_server.py`
  - Added `GET /sessions/{id}/runtime-metrics?steps=N`.
  - Response includes compact metric rows, latest row, and a summary with total LLM tokens, max latency, token cache, retrieval, and tool-ranking state.
  - `/agent/handoff` now includes `runtime_metrics_summary` so external agents get diagnostics in the first bundle.
- `core/runtime_contract.py`
  - Added `communication.runtime_metrics`.
  - External attach sequence now points LLMs to runtime metrics when diagnosing latency/token cost.
  - Token mechanisms now include the runtime metrics pull API.
- `frontend/src/components/AgentWorkbench.tsx`
  - Runtime Contract panel shows the runtime metrics endpoint.
  - Handoff preview shows a compact Runtime summary next to Context and Skills.
- `tests/test_tool_call_validation.py`
  - Removed the unreachable mojibake fallback block in the tool-ranking cache test.

Verification:
- `python -m py_compile server/session_store.py server/app_server.py core/runtime_contract.py tests/test_app_server_resume.py tests/test_runtime_contract.py tests/test_tool_call_validation.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_tool_call_validation.py`
- `python tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`

Next:
- Use `/sessions/{id}/runtime-metrics` after a real API-backed GUI task to choose the next latency fix from evidence instead of guessing.

## Async Learning Layer And Layer Event Bridge

Moved learning reflection out of the user-facing completion path and fixed the layer-event bridge into AppServer.

Delivered:
- `agent/agent.py`
  - Added `event_sink` for Agent-level events so `layer` transitions reach AppServer/SSE/session persistence, not only the in-memory agent queue.
  - Replaced synchronous post-run learning with `_dispatch_learning_job()`.
  - Learning jobs now track `queued/running/done/error/skipped`, `async`, timestamps, elapsed time, and errors.
  - Default behavior is async when eligible (`learning.async_enabled`, default true); ineligible short runs emit `learning.reflection.skipped`.
  - `result["learning_job"]` records the initial learning state returned to callers.
- `server/app_server.py`
  - Wires `session_agent.event_sink` to the session emitter so Agent/Core/Learning layer events are persisted and visible through the same channel as ReAct events.
  - Added `GET /sessions/{id}/learning?steps=N`.
  - `/agent/handoff` now includes `learning_latest`.
- `server/session_store.py`
  - Added `get_learning_history()` over persisted `layer` events.
- `core/runtime_contract.py`
  - Added `communication.learning_status`.
  - External attach sequence now points to learning status when checking background learning.
- `frontend/src/components/AgentWorkbench.tsx`
  - Runtime Contract panel shows the learning status endpoint.
  - Handoff preview shows compact Learning status.
- `tests/test_layer_events.py`
  - Updated skipped-learning expectations for short runs.
  - Added proof that eligible learning runs asynchronously and completes after `agent.run()` returns.
- `tests/test_app_server_resume.py`
  - Added proof that AppServer persists layer events and exposes learning status.

Verification:
- `python -m py_compile agent/agent.py server/app_server.py server/session_store.py core/runtime_contract.py tests/test_layer_events.py tests/test_app_server_resume.py tests/test_runtime_contract.py`
- `python tests/test_layer_events.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`

Next:
- Use a real API-backed Maker session to decide whether the remaining speed gap is provider latency, prompt weight, MakerMCP tool execution, or repair flow.

## Maker First-Action Briefing

Turned MakerMCP onboarding from a passive contract/checklist into a compact next-action surface that LLMs can call directly.

Delivered:
- `core/runtime_contract.py`
  - Added `build_maker_briefing(contract, task)`.
  - Briefing selects the most relevant Maker task template from the current task.
  - Returns readiness, authority, selected template, recommended first action, endpoint, suggested tools, checklist, and evidence endpoints.
  - Runtime Contract now exposes `communication.maker_briefing`.
  - External attach sequence now tells agents to fetch maker briefing before the first Maker action.
  - Token mechanisms include `maker_briefing next-action API`.
- `agent/agent.py`
  - Added Agent-callable `maker_briefing` tool.
  - Added `TapMakerAgent.maker_briefing(session_id, task)`.
- `server/app_server.py`
  - Added `GET /agent/maker-briefing?session_id=...&task=...`.
  - `/agent/handoff` now includes `maker_briefing`.
- `frontend/src/components/AgentWorkbench.tsx`
  - Runtime Contract panel shows the Maker briefing endpoint.
  - Handoff preview shows a compact Maker summary: readiness, authority, selected flow, and suggested tools.

Verification:
- `python -m py_compile core/runtime_contract.py agent/agent.py server/app_server.py tests/test_runtime_contract.py tests/test_layer_events.py tests/test_app_server_resume.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_layer_events.py`
- `python tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`

Next:
- Run a real API-backed Maker task and inspect whether the LLM actually calls/uses maker briefing; if not, inject briefing as a first-step guard before action selection.

## Maker Briefing First-Step Injection

Made Maker briefing operational at session start instead of only exposing it as a callable endpoint/tool.

Delivered:
- `core/runtime_contract.py`
  - Added `render_maker_briefing_for_llm()` to compact the live briefing into a small prompt block.
- `agent/react_loop.py`
  - Builds `_maker_briefing_snapshot` from the Runtime Contract at session start.
  - Injects `[maker_briefing]...[/maker_briefing]` into the first LLM context.
  - Emits a persisted `maker_briefing` event before the first goal/context sync events, so SSE replay and Workbench can see the first-action guard.
- `frontend/src/hooks/useBackend.ts`
  - Added `WorkbenchMakerBriefing` state and consumes `maker_briefing` events without adding rows to the main chat transcript.
- `frontend/src/components/AgentWorkbench.tsx`
  - Added a dedicated Maker First Action surface showing authority, selected template, recommended next action, suggested tools, evidence endpoints, and acceptance hints.
- `frontend/src/styles/index.css`
  - Added dense, wrapping TapTapMaker-style styling for the briefing panel with no fixed-height clipping.
- `tests/test_runtime_contract.py`
  - Proves ReAct context includes `[maker_briefing]`.
  - Proves Maker briefing can be built/emitted and rendered within a compact size budget.

Verification:
- `python -m py_compile core/runtime_contract.py agent/react_loop.py tests/test_runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_layer_events.py`
- `python tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`

Next:
- Run a real API-backed Maker session and inspect whether the first action follows `maker_briefing`; if it still drifts, add a stricter first-action guard before tool selection.

## Maker Briefing First-Action Guard

Turned the injected Maker briefing into an enforceable first-action guard for arbitrary LLM providers.

Delivered:
- `agent/react_loop.py`
  - Added `_maker_first_action_guard()` before tool preflight/execution.
  - On the first iteration, if MakerMCP is connected and the model tries a local side effect such as write/delete/build/submit/publish before using Maker authority, ReAct blocks that step.
  - Emits `maker_briefing_guard` events with `pass/warn/block`, authority, allowed tools, suggested tools, recommended first action, and endpoint.
  - Adds `[maker_briefing_guard]` context hints so the next LLM step can self-correct instead of repeating the same bad action.
- `frontend/src/hooks/useBackend.ts`
  - Added `makerGuard` Workbench state and consumes `maker_briefing_guard` events without adding main-chat rows.
- `frontend/src/components/AgentWorkbench.tsx`
  - Added `First Action Guard` panel showing alignment/block status, tool, reason, suggested tools, and endpoint.
- `frontend/src/styles/index.css`
  - Added compact wrapping styles for pass/warn/block guard states.
- `tests/test_tool_call_validation.py`
  - Added a ReAct integration test proving a first local `write_file` is blocked when Maker briefing points to `maker_build`, and the next `maker_build` action can proceed.

Verification:
- `python -m py_compile agent/react_loop.py core/runtime_contract.py tests/test_tool_call_validation.py tests/test_runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_tool_call_validation.py`
- `npm.cmd --prefix frontend run build`

Partial verification blocked:
- `tests/test_app_server_resume.py` and `tests/test_layer_events.py` could not complete because the Windows environment reported `WinError 112` disk space exhaustion while writing temp/log files.

Next:
- Free disk space, rerun AppServer/layer regression, then run a real API-backed Maker task and inspect `maker_briefing_guard` decisions from Workbench/runtime history.

## Pullable Maker Guard History And Handoff Evidence

Made Maker first-action alignment available to external LLMs/processes without replaying raw SSE.

Delivered:
- `server/session_store.py`
  - Added `get_maker_guard_history(session_id, limit)` to compact persisted `maker_briefing_guard` events.
  - Returns decision, tool, reason, authority, selected template, allowed/suggested tools, recommended first action, endpoint, and timestamp.
- `server/app_server.py`
  - Added `GET /sessions/{id}/maker-guard?steps=N`.
  - `/agent/handoff` now includes `maker_guard_latest` and compact `maker_guard` history.
- `core/runtime_contract.py`
  - Added `communication.maker_guard`.
  - External attach sequence now points to maker-guard history when checking first-action Maker alignment.
  - Maker briefing evidence endpoints now include `maker_guard`.
  - Prompt renderer now compacts communication/external-agent data so `external_agent_handoff` remains visible under small prompt caps after adding more endpoints.
- `tests/test_runtime_contract.py`
  - Covers `maker_guard` communication/evidence endpoint and prompt-size rendering.
- `tests/test_app_server_resume.py`
  - Added focused maker-guard endpoint test.
  - Handoff test now verifies `maker_guard_latest`.

Verification:
- `python -m py_compile core/runtime_contract.py server/session_store.py server/app_server.py tests/test_runtime_contract.py tests/test_app_server_resume.py`
- `python tests/test_runtime_contract.py`
- Focused `test_app_server_maker_guard_endpoint`
- Focused `test_app_server_external_agent_handoff_endpoint`

Next:
- Rerun the broad AppServer/layer tests once disk pressure is gone, then use a real API-backed Maker task to inspect `maker_briefing`, `maker_briefing_guard`, runtime metrics, and learning status together.

## Workbench Maker Guard Evidence Loop

Closed the frontend/backend loop for Maker first-action guard evidence.

Delivered:
- `frontend/src/components/AgentWorkbench.tsx`
  - Handoff preview now understands `maker_guard_latest`.
  - Runtime Contract panel displays the `maker_guard` endpoint.
  - Handoff preview now shows a compact `Guard` row with decision, tool, authority, selected Maker flow, suggested tools, and reason.

Verification:
- `npm.cmd --prefix frontend run build`
- `python tests/test_runtime_contract.py`
- Focused maker-guard endpoint + handoff endpoint tests
- `python tests/test_app_server_resume.py`
- `python tests/test_layer_events.py`

Result:
- Previous `WinError 112` disk-space blocker did not recur in this run; broad AppServer/layer regressions passed.

Next:
- Run a real GUI/API-backed Maker task and inspect the four live surfaces together: `maker_briefing`, `maker_guard`, `runtime_metrics`, and `learning`.

## Runtime Advice For Evidence-Driven Next Actions

Added a compact diagnostic layer over existing runtime evidence so external LLMs do not need to infer the next fix from four separate surfaces.

Delivered:
- `server/app_server.py`
  - Added `build_runtime_advice()` deriving priority and next action from `maker_briefing`, `maker_guard`, `runtime_metrics`, `learning`, and `context_sync`.
  - Added `GET /sessions/{id}/runtime-advice?steps=N`.
  - `/agent/handoff` now includes `runtime_advice`.
  - Advice priorities currently cover `maker_alignment`, `maker_mcp_connection`, `latency`, `token_efficiency`, `learning_async`, `context_sync`, and `continue`.
- `core/runtime_contract.py`
  - Added `communication.runtime_advice`.
  - External attach sequence points agents to runtime advice for the next diagnostic action.
  - Compact prompt rendering includes `runtime_advice` without bloating the full contract.
- `frontend/src/components/AgentWorkbench.tsx`
  - Runtime Contract panel shows the Advice endpoint.
  - Handoff preview shows an `Advice` row with status, priority, next action, and the leading reason.
- `tests/test_app_server_resume.py`
  - Added focused runtime-advice endpoint test proving blocked Maker guard becomes `priority=maker_alignment`.
  - Handoff test verifies runtime advice is present and actionable.
- `tests/test_runtime_contract.py`
  - Covers `runtime_advice` communication and attach sequence.

Verification:
- `python -m py_compile server/app_server.py core/runtime_contract.py tests/test_runtime_contract.py tests/test_app_server_resume.py`
- `python tests/test_runtime_contract.py`
- `npm.cmd --prefix frontend run build`
- Focused runtime-advice + handoff endpoint tests
- `python tests/test_app_server_resume.py`
- `python tests/test_layer_events.py`

Next:
- Run a real GUI/API-backed Maker task. Inspect `runtime_advice` first, then drill into maker_guard/runtime_metrics/learning only when advice points there.

## External LLM Quickstart Bundle

Added a single startup packet for arbitrary external coding agents so they can attach to TTMEvolve without reconstructing the operating protocol from multiple endpoints.

Delivered:
- `core/runtime_contract.py`
  - Added `communication.quickstart_bundle`.
  - External attach sequence now starts with `GET /agent/quickstart?session_id=...&steps=3`.
  - Compact prompt communication includes `quickstart_bundle`.
- `server/app_server.py`
  - Added `build_llm_quickstart_bundle()`.
  - Added `GET /agent/quickstart?session_id=...&steps=N`.
  - Quickstart returns a short external-agent prompt, boot sequence, Maker readiness, runtime advice, Maker briefing, latest context sync, compact endpoints, and operating rules.
- `frontend/src/components/AgentWorkbench.tsx`
  - Runtime Contract panel now displays the Quickstart endpoint.
- `tests/test_runtime_contract.py`
  - Covers `quickstart_bundle` communication and quickstart-first attach sequence.
- `tests/test_app_server_resume.py`
  - Added focused external LLM quickstart endpoint test.
  - Handoff test updated for quickstart-first attach sequence.

Verification:
- `python -m py_compile server/app_server.py core/runtime_contract.py tests/test_runtime_contract.py tests/test_app_server_resume.py`
- `python tests/test_runtime_contract.py`
- `npm.cmd --prefix frontend run build`
- Focused quickstart + handoff endpoint tests
- `python tests/test_app_server_resume.py`
- `python tests/test_layer_events.py`

Next:
- Use `/agent/quickstart` as the first handoff surface for real Claude/Codex/opencode-style LLM sessions, then inspect whether the first action follows runtime advice and Maker briefing.

## Workbench Quickstart Preview And Prompt Copy

Made the external LLM quickstart bundle directly usable from the Workbench instead of only exposing a URL.

Delivered:
- `frontend/src/components/AgentWorkbench.tsx`
  - Added Quickstart preview state and loader.
  - Runtime Contract panel now shows an `LLM Quickstart` block with Preview and Copy Prompt controls.
  - Preview displays advice summary, Maker summary, prompt, and first boot sequence steps.
- `frontend/src/styles/index.css`
  - Added dense mint-styled Quickstart block styles with wrapping text and no fixed-height clipping.

Verification:
- `npm.cmd --prefix frontend run build`
- Focused quickstart + handoff endpoint tests
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_layer_events.py`

Next:
- Use the Workbench Quickstart Preview to copy the startup prompt into a real external LLM and inspect whether its first action follows runtime advice, Maker briefing, and Maker guard.

## Markdown Quickstart Prompt

Upgraded the quickstart copy target from a short sentence to a structured Markdown startup card.

Delivered:
- `server/app_server.py`
  - Added `render_quickstart_markdown()`.
  - `/agent/quickstart` now returns `prompt_markdown`.
  - Markdown includes session/task, runtime advice, Maker authority, boot sequence, compact endpoints, and operating rules.
- `frontend/src/components/AgentWorkbench.tsx`
  - Quickstart preview and Copy Prompt now prefer `prompt_markdown` and fall back to the short prompt.
- `tests/test_app_server_resume.py`
  - Quickstart endpoint test now verifies the Markdown prompt includes the title, Runtime Advice section, and runtime advice endpoint.

Verification:
- `python -m py_compile server/app_server.py tests/test_app_server_resume.py`
- Focused quickstart markdown endpoint test
- `npm.cmd --prefix frontend run build`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_layer_events.py`

Next:
- Paste the Markdown quickstart into a real external/API-backed LLM and compare its first action against runtime advice, Maker briefing, and maker guard.

## Direct Markdown Quickstart Endpoint

Made the external LLM startup card fetchable directly as Markdown, not only as JSON plus `prompt_markdown`.

Delivered:
- `server/app_server.py`
  - Added `_text_response()`.
  - `/agent/quickstart?format=markdown` and `/agent/quickstart.md` now return `text/markdown`.
  - JSON quickstart remains unchanged for the GUI.
- `frontend/src/components/AgentWorkbench.tsx`
  - Quickstart block now computes a Markdown URL.
  - Added Copy MD URL action.
- `frontend/src/styles/index.css`
  - Added wrapping style for the Markdown quickstart URL line.
- `tests/test_app_server_resume.py`
  - Quickstart endpoint test verifies both `?format=markdown` and `.md` return the same Markdown startup card.

Verification:
- `python -m py_compile server/app_server.py tests/test_app_server_resume.py`
- Focused quickstart direct Markdown endpoint test
- `npm.cmd --prefix frontend run build`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_layer_events.py`

Next:
- For a real external LLM, prefer handing it the Markdown URL when it can fetch local URLs; otherwise use Copy Prompt.

## Quickstart Surface Profiles

Added ecosystem-specific quickstart profiles so external agents do not need to guess how to map TTMEvolve startup rules into their native memory/skill conventions.

Delivered:
- `server/app_server.py`
  - Added `quickstart_surface_profile()`.
  - `/agent/quickstart` and `/agent/quickstart.md` accept `surface=generic|codex|claude-code|opencode`.
  - Quickstart JSON now returns `surface`.
  - Markdown now includes a `Surface Profile` section with memory files, start rule, and skill style.
  - Profile-specific boot sequence preserves `surface=...` for refreshes.
- `frontend/src/components/AgentWorkbench.tsx`
  - Quickstart preview now shows a Surface summary.
- `tests/test_app_server_resume.py`
  - Quickstart endpoint test covers `surface=codex`, Markdown surface output, and profile-specific boot sequence.

Verification:
- `python -m py_compile server/app_server.py tests/test_app_server_resume.py`
- Focused quickstart surface profile endpoint test
- `npm.cmd --prefix frontend run build`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_layer_events.py`

Next:
- Add optional UI surface selector if real external-agent use shows users need to switch between Codex/Claude/opencode frequently.

## Workbench Surface Selector And API Runtime Evidence

- AgentWorkbench Quickstart now has a compact surface selector for `generic`, `codex`, `claude-code`, and `opencode`.
- Quickstart JSON and Markdown URLs now carry the selected `surface=...` parameter, and changing surface clears stale preview/copy state.
- Copy Prompt no longer requires Preview first; it fetches the current Markdown quickstart card directly when needed.
- Verified MiniMax is not a dead route: `LLMFactory.create("minimax")` instantiates `MiniMaxLLM`, API tests cover its request diagnostics, and the default endpoint is `https://api.minimax.chat/v1/text/chatcompletion_v2`.
- `/health` now exposes `runtime_kind`, `model`, `base_url`, `api_key_set`, and `last_call_stats.endpoint`, so GUI/runtime diagnostics can prove whether an API provider such as MiniMax is actually being called.
- Cockpit Runtime status now treats API providers as `api ready` / `needs key` instead of local GGUF `loaded` / `cold`; hover text includes provider, kind, model, base URL, key state, and last endpoint.

Verification:
- `npm.cmd --prefix frontend run build`
- `python -m py_compile server/app_server.py tests/test_app_server.py llm/minimax_llm.py`
- `python tests/test_app_server.py`
- `python tests/test_api_llm_errors.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_layer_events.py`

Next:
- Run a real API-backed Maker task with MiniMax or another configured provider and compare `/health.last_call_stats.endpoint`, runtime_advice, maker_guard, runtime_metrics, and learning status before choosing the next optimization.

## Active LLM Runtime Probe

- Added `POST /llm/probe`.
  - Uses a cloned LLM config, so it can test the selected provider/model/base URL/API key without mutating the running agent.
  - Makes one tiny request when the provider client exposes `_call`, capped to `max_tokens=16`, and returns `ok`, provider, runtime kind, class, model, base URL, elapsed time, output preview, and `last_call_stats`.
  - Stores the latest probe on `server.last_llm_probe`; `/health` now includes `last_probe` for passive diagnostics.
- Provider config now uses `_apply_llm_values_to_config()` in `/config/llm`, so provider-scoped `llm.api_keys` are persisted and switching providers resets model/base URL to the new preset unless the UI sends explicit values.
- OpenAI-compatible and Claude clients now include `endpoint` and `http_status` in `last_call_stats`, matching MiniMax diagnostics.
- ProviderSelector now has a `Probe` button and a compact result row showing API verified/failed, provider class/model, elapsed time, endpoint, token count, and error type.
- Added AppServer smoke coverage for `/llm/probe` with `mock`, so the endpoint is tested without network calls.

Verification:
- `npm.cmd --prefix frontend run build`
- `python -m py_compile server/app_server.py llm/openai_llm.py llm/claude_llm.py llm/minimax_llm.py tests/test_app_server.py`
- `python tests/test_app_server.py`
- `python tests/test_api_llm_errors.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_layer_events.py`
- `python tests/test_tool_call_validation.py`

Next:
- Use Probe with a real MiniMax/API key to confirm endpoint/tokens/latency, then run a minimal Maker task and inspect runtime_advice, maker_guard, runtime_metrics, and learning before the next optimization.

## LLM Probe Evidence Loop

- Runtime Contract communication now exposes `llm_probe: /llm/probe`.
- External-agent attach sequence now tells agents to `POST /llm/probe` when provider wiring or API latency is uncertain.
- Runtime Contract prompt rendering keeps `llm_probe` while shrinking compact communication so `external_agent_handoff` remains visible under the 900-char cap.
- `build_runtime_advice()` now accepts `llm_probe_latest`.
  - Missing probe is non-blocking.
  - Failed probe returns `priority=llm_provider` with provider/API error evidence before Maker/prompt debugging.
- Quickstart JSON/Markdown now includes an `LLM Runtime` section and compact `llm_probe` summary.
- Handoff bundle now includes `llm_probe_latest`.
- AgentWorkbench Quickstart and Handoff previews now show a compact LLM row with probe status, provider, model, endpoint/base URL, latency, tokens, and error.

Verification:
- `python -m py_compile server/app_server.py core/runtime_contract.py tests/test_app_server_resume.py tests/test_runtime_contract.py`
- `npm.cmd --prefix frontend run build`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_app_server.py`
- `python tests/test_layer_events.py`
- `python tests/test_tool_call_validation.py`
- `python tests/test_api_llm_errors.py`

Next:
- Run ProviderSelector Probe with a real MiniMax/API key, then run one small Maker task; compare probe/advice/guard/metrics/learning before deciding whether the next optimization is provider routing, prompt size, MakerMCP tool selection, or context compression.

## Session-Scoped LLM Probe History

- `POST /llm/probe` now accepts optional `session_id`.
  - When provided, the probe result is appended to SessionStore as an `llm_probe` event.
  - The probe still uses cloned config and does not leak API keys.
- Added `SessionStore.get_llm_probe_history(session_id, limit)`.
- Added `GET /sessions/{id}/llm-probe?steps=N`.
  - Returns compact probe rows with provider, runtime kind, class, model, base URL, endpoint, HTTP status, token count, latency, output preview, and error type.
- Runtime Contract communication now includes `llm_probe_history`.
- External-agent attach sequence now includes the session probe history endpoint.
- Runtime Advice, Quickstart, and Handoff now prefer session-scoped probe evidence over global `server.last_llm_probe`.
- AppServer smoke test now posts `/llm/probe` with a session id and verifies the session history endpoint.

Verification:
- `python -m py_compile server/app_server.py server/session_store.py core/runtime_contract.py tests/test_session_store.py tests/test_app_server.py tests/test_app_server_resume.py tests/test_runtime_contract.py`
- `npm.cmd --prefix frontend run build`
- `python tests/test_session_store.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_layer_events.py`
- `python tests/test_api_llm_errors.py`
- `python tests/test_tool_call_validation.py`

Next:
- Pass active `session_id` from the GUI ProviderSelector Probe button when a session exists, then run a real MiniMax/API-backed Maker task and inspect `/sessions/{id}/llm-probe`, runtime_advice, maker_guard, runtime_metrics, and learning.
## Live Session-Scoped LLM Probe Evidence

- ProviderSelector Probe now receives `sessionId` from `ChatPanel`/`useBackend` and sends `session_id` to `POST /llm/probe` when a session exists.
- Provider config controls are locked during an active run, but Probe remains available so the user can verify the current provider without mutating runtime settings.
- Active-session probes now call `Session.emit({"type": "llm_probe", ...})`, which keeps SSE, SessionStore persistence, and Workbench runtime state aligned. Inactive/persisted sessions still append directly to SessionStore.
- `AgentWorkbenchState` now includes `llmProbe`; `useBackend` consumes live `llm_probe` events and updates Runtime layer metrics; Workbench displays a compact non-clipping probe evidence row.
- `SessionStore.create_session()` now deletes old events for the same `session_id` before replacing session metadata, preventing stale probe/history data from fixed-id smoke sessions.
- Verification passed: frontend build, Python py_compile for app/session store, `tests/test_app_server.py`, `tests/test_session_store.py`, `tests/test_app_server_resume.py`, `tests/test_runtime_contract.py`, `tests/test_layer_events.py`.
- Next: run a real API-backed Maker task and compare live `llm_probe`, `/sessions/{id}/llm-probe`, runtime_advice, maker_guard, runtime_metrics, and learning before optimizing latency further.

## Live Runtime Advice Workbench Panel

- AgentWorkbench Runtime Contract panel now computes a local `adviceUrl` from `communication.runtime_advice` or `/sessions/{id}/runtime-advice?steps=20`.
- Runtime Advice auto-refreshes after key evidence changes: `llmProbe`, Maker guard, context sync revision, goal checklist status, total latency, and learning-layer event.
- The panel keeps a manual Refresh button and displays compact `status`, `priority`, `next_action`, reasons, and summarized evidence keys instead of raw JSON.
- Styling is compact, wrapping, and priority-colored: provider/Maker alignment/MakerMCP failures are danger, latency/token/context watch states are warning, continue/ready states are success.
- This closes a frontend/backend gap: users and external LLMs no longer have to manually preview Handoff/Quickstart to see the current next diagnostic action.
- Verification passed: `npm.cmd --prefix frontend run build`, `tests/test_app_server_resume.py`, `tests/test_runtime_contract.py`.
- Next: run a real API-backed Maker session and use the live Runtime Advice panel as the first decision surface before drilling into guard/metrics/learning endpoints.

## Live Runtime Metrics Workbench Panel

- AgentWorkbench Runtime Contract panel now computes `metricsUrl` from `communication.runtime_metrics` or `/sessions/{id}/runtime-metrics?steps=20`.
- Runtime Metrics auto-refreshes from runtime-layer evidence changes, including runtime layer event and first/LLM/tool/total latency updates.
- The panel shows:
  - event count
  - total LLM tokens
  - token cache hits/misses/size
  - tool ranking selected/candidates/cache-hit
  - max latency, context build time, and latest metric details
- Handoff Runtime summary now reuses the same `runtimeMetricsSummary()` helper so Handoff and live Workbench do not drift.
- Styling is a compact blue instrument panel, wrapping long values and avoiding fixed-height clipping.
- Verification passed: `npm.cmd --prefix frontend run build`, `tests/test_app_server_resume.py`, `tests/test_runtime_contract.py`.
- Next: use a real API-backed Maker task to decide from live numbers whether the next optimization should target provider latency, prompt/context pressure, tool ranking, or cache behavior.

## Live Learning Status Workbench Panel

- AgentWorkbench Runtime Contract panel now computes `learningUrl` from `communication.learning_status` or `/sessions/{id}/learning?steps=20`.
- Learning Status auto-refreshes when session stage or Learning layer status/event/timestamp changes.
- The panel shows latest learning/reflection event, state, event count, detail, and compact metrics such as async/elapsed/trajectory/skill/knowledge/error counts.
- Styling is compact, green/mint, and non-clipping, with skipped/error/running states differentiated.
- This makes the third architecture layer visible as an independent async process instead of only a layer pill or handoff summary.
- Verification passed: `npm.cmd --prefix frontend run build`, `tests/test_layer_events.py`, `tests/test_app_server_resume.py`.
- Next: run a real API-backed Maker session and inspect LLM Probe + Runtime Advice + Runtime Metrics + Learning Status + Maker Guard together.

## External Agent Boot Links Panel

- AgentWorkbench now shows an `External Agent Boot` panel before Quickstart/Handoff details.
- The panel summarizes which boot surfaces are ready and provides one-click `Copy Boot`.
- Copied boot checklist includes:
  - Quickstart Markdown/JSON
  - Handoff Bundle
  - Runtime Advice
  - Maker Briefing
  - Maker Guard
  - Runtime Metrics
  - Learning Status
  - Context Sync
  - LLM Probe History
- This directly supports arbitrary LLM onboarding: users can paste one compact checklist into Claude/Codex/opencode/other agents without assembling URLs manually.
- Styling is compact mint/blue, wrapping long local URLs and avoiding fixed-height clipping.
- Verification passed: frontend build, Runtime Contract tests, AppServer resume tests.
- Next: use this boot checklist with a real external/API-backed LLM run and inspect whether the first action follows Quickstart -> Advice -> Maker Briefing -> Guard.

## Compact Evidence Bundle

- Added `GET /sessions/{id}/evidence?steps=N` as the preferred first evidence pull for attached LLMs and external agents.
- The bundle returns compact current state only: `runtime_advice`, `maker_briefing`, latest `context_sync`, `runtime_metrics_summary`, latest Learning event, latest Maker guard, compact LLM probe, counts, endpoint map, and a token rule.
- Runtime Contract now exposes `communication.evidence_bundle` and puts it second in external attach sequence, immediately after Quickstart and before detailed Handoff.
- Quickstart boot sequence and endpoint map include `evidence_bundle`.
- AgentWorkbench External Agent Boot now copies Evidence Bundle as step 2 and labels Handoff as a deeper pull only when evidence is insufficient.
- This reduces cross-agent startup latency and token waste by avoiding default raw SSE or multi-endpoint replay.

Verification:
- `python -m py_compile server/app_server.py core/runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`

Next:
- Run a real API-backed Maker task, copy External Agent Boot, have the attached LLM read Evidence Bundle first, and compare whether it can act without pulling Handoff or raw histories.

## Evidence Bundle-Driven Workbench Refresh

- AgentWorkbench now uses `/sessions/{id}/evidence?steps=20` as its automatic refresh path when the Runtime Contract exposes `communication.evidence_bundle`.
- A single bundle fetch hydrates:
  - Runtime Advice
  - Runtime Metrics summary/count
  - Learning latest/count
- The previous detail endpoints remain available through manual Refresh buttons, so users can drill down when the bundle is insufficient.
- External Agent Boot now includes a compact Bundle status row with advice, context revision/count, metrics count, learning state, guard status, and probe status.
- This moves the frontend closer to the intended cockpit model: one compact runtime evidence pull for normal operation, focused detail pulls only on demand.

Verification:
- `npm.cmd --prefix frontend run build`
- `python -m py_compile server/app_server.py core/runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`

Next:
- Run one real API-backed Maker task and compare network/runtime behavior: Workbench should refresh from Evidence Bundle first, while manual Advice/Metrics/Learning refresh remains available for detail diagnosis.

## Pasteable Evidence Markdown

- Added `render_session_evidence_markdown()` for the compact Evidence Bundle.
- `GET /sessions/{id}/evidence?format=markdown` and `GET /sessions/{id}/evidence.md` now return a pasteable Markdown card.
- The card includes session/task, next action, Maker authority, latest context/guard/learning/probe/runtime metrics, counts, detail endpoints, and token rule.
- AgentWorkbench External Agent Boot now has `Copy Evidence`, which copies the Markdown card itself rather than only a localhost URL.
- Boot checklist prefers Evidence MD when available, while JSON Evidence remains the automatic Workbench refresh path.
- This closes an onboarding gap for external LLMs that cannot access local TTMEvolve URLs directly.

Verification:
- `python -m py_compile server/app_server.py tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`

Next:
- In a real API-backed Maker session, use Copy Evidence with an external LLM that cannot fetch localhost and check whether it can choose the correct first Maker-aligned action without Handoff/raw histories.

## Three-Layer Evidence Summary

- Added `SessionStore.get_layer_history(session_id, limit)` for compact persisted layer communication events across Agent, Runtime, Learning, user, and storage routes.
- Added `summarize_layer_events()` and included `layer_summary` in `/sessions/{id}/evidence`.
- Evidence Bundle now reports:
  - total layer event count
  - latest event/state/route for each layer
  - recent source->target routes
- Evidence Markdown now includes a `Layer Communication` section for agent/runtime/learning latest state, event, and route.
- Workbench Evidence Bundle summary line now includes layer event count/current layer state.
- This makes the three-layer architecture visible in the same compact artifact external LLMs already use, instead of requiring raw SSE replay.

Verification:
- `python -m py_compile server/app_server.py server/session_store.py tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`

Next:
- Use a real task to verify whether external LLMs can infer whether the next fix belongs to Agent planning, Runtime/MakerMCP, or Learning from Evidence Markdown alone.

## MakerMCP Authority Evidence And styles.css Alignment

- Evidence Bundle now includes `maker_mcp` summary from Runtime Contract:
  - readiness
  - connected
  - tool_count
  - top_tools
  - remote_identity
  - last_call
- Evidence Markdown separates MakerMCP runtime state from Maker briefing:
  - `mcp_readiness`
  - `mcp_connected`
  - `mcp_tool_count`
  - `remote_identity`
  - `last_call`
  - `mcp_top_tools`
- Workbench Bundle summary now includes `maker={readiness}:{tool_count}`.
- Read and learned the referenced `styles.css` design language:
  - brand `#00D9C5` / `#00CDBA`
  - app surface `#F7F9FA`
  - panel white / raised surfaces
  - text `#060A26`, muted text as rgba
  - 6/8/10/16px radius scale
  - subtle panel shadows
  - thin scrollbars
  - teal focus ring `rgba(0,217,197,0.16)`
- Aligned `frontend/src/styles/index.css` global tokens to that visual language.

Verification:
- `npm.cmd --prefix frontend run build`
- `python -m py_compile server/app_server.py tests/test_app_server_resume.py`
- `python tests/test_app_server_resume.py`
- `python tests/test_runtime_contract.py`

Next:
- Continue replacing ad hoc panel colors with the learned token system, then run a real API/Maker session and test whether Evidence Markdown is enough for an external LLM to choose MakerMCP-first actions.

## Workbench Semantic Token Cleanup

- Added semantic frontend tokens for TapTapMaker-style Workbench surfaces:
  - primary/mint text, border, surface
  - info panel/soft/border
  - warning/danger/success border and soft states
  - reusable white panel surfaces
- Replaced hardcoded Workbench diagnostic colors across:
  - goal/preflight/plan/commit state
  - Runtime Contract and External Agent Boot
  - Quickstart surface selector
  - Maker Briefing, Maker Guard, LLM Probe
  - Runtime Advice, Runtime Metrics, Learning Status
  - Maker checklist/templates, Context Sync, Skill Sync
- Changed `.workbench-error` from a clipped 58px block to full wrapping text with `overflow: visible`, `overflow-wrap: anywhere`, and `white-space: pre-wrap`.
- Tokenized the main chat/event rails enough to preserve full-content agent transcript behavior while matching the same mint/info/danger state language.

Verification:
- `npm.cmd --prefix frontend run build`

Next:
- Run a real API-backed Maker task and inspect `/sessions/{id}/evidence?steps=20`, `/sessions/{id}/evidence.md`, Provider Probe endpoint stats, Runtime Advice, Maker Guard, Runtime Metrics, and Learning before choosing the next optimization.

## v0.4.1 Runtime Readiness Checkpoint

- Added `GET /runtime/readiness?session_id=...` as the first no-network runtime diagnosis gate for users and attached LLMs.
- Runtime Readiness reports:
  - provider/runtime kind/model/base URL/API-key presence
  - latest session-scoped LLM Probe status and endpoint
  - MakerMCP readiness, connected state, tool count, remote identity, and last call
  - layer summary, context sync, runtime metrics, learning latest, Maker guard latest
  - `blocked` / `degraded` / `ready` status
  - release gate checks for provider config, provider probe, MakerMCP evidence, layer evidence, and context sync
  - next actions that avoid raw SSE and unnecessary token spend
- Runtime Contract now exposes `communication.runtime_readiness` and puts it first in `external_agents.attach_sequence`.
- Quickstart boot sequence now starts with Runtime Readiness, then Quickstart/Evidence.
- Workbench External Agent Boot now includes Runtime Readiness in the ready summary and copied checklist.
- Added release note: `docs/releases/v0.4.1-runtime-readiness.md`.

Verification:
- `python -m py_compile server/app_server.py core/runtime_contract.py tests/test_app_server_resume.py tests/test_runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`

Stable small-version boundary:
- This is a stable diagnostic/onboarding checkpoint, not the final full product goal.
- Next real gate is a live API-backed Maker task with Provider Probe, Runtime Readiness, Evidence Bundle, Runtime Advice, Maker Guard, Runtime Metrics, and Learning inspected together.
