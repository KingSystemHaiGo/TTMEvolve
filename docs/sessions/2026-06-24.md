# Session 2026-06-24

## v0.4.1 Closure Audit And Metadata Sync

- Continued the active goal toward closure rather than adding new feature scope.
- Added `docs/releases/v0.4.1-completion-audit.md`, a requirement-by-requirement audit for the user's six-part objective.
- Audit conclusion:
  - `v0.4.1-runtime-readiness` is a verified stable small version.
  - The broader long-term objective is not fully proven until a real API-backed Maker session validates provider Probe, MakerMCP remote authority, Maker Guard, Runtime Readiness/Evidence, Runtime Metrics, Learning, and GUI behavior.
- Synced package metadata to `0.4.1-runtime-readiness` in:
  - `package.json`
  - `package-lock.json`
  - `frontend/package.json`
  - `frontend/package-lock.json`
  - `electron/package.json`
  - `electron/package-lock.json`
- Updated `docs/memory-index.md` top version to `v0.4.1-runtime-readiness`.
- Updated `docs/releases/v0.4.1-runtime-readiness.md` with completion audit link, Electron build gate, and package metadata note.

Verification:
- `python -m py_compile server/app_server.py core/runtime_contract.py tests/test_app_server_resume.py tests/test_runtime_contract.py`
- `python tests/test_runtime_contract.py`
- `python tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Next:
- Do not mark the full thread goal complete yet. Run one real API-backed Maker validation pass:
  1. Provider Probe with real key.
  2. Small Maker task.
  3. Runtime Readiness.
  4. Evidence Bundle.
  5. Runtime Advice, Maker Guard, Runtime Metrics, Learning Status.
  6. GUI preview/chat usability check.

## API Call Proof And Local LLM Feedback Summary

- Added no-network `llm_call_proof` evidence to Runtime Readiness and Evidence Bundle.
- The proof summarizes provider/runtime/model/base URL, expected endpoint, observed endpoint, evidence source, endpoint match, API-key state, and conclusion.
- MiniMax proof explicitly expects `/text/chatcompletion_v2`; OpenAI-compatible providers expect `/chat/completions`; Claude expects `/messages`.
- Added `GET /llm/feedback-summary` to read saved `docs/llm-feedback` artifacts without sending project data to an external LLM.
- Runtime Contract communication and External Agent attach sequence now expose `llm_feedback_summary`.
- Evidence JSON/Markdown includes `llm_call_proof` and local feedback summary, so users/external agents can distinguish "configured but not observed" from "real endpoint observed."
- Workbench Evidence summary now shows API proof and feedback counts/status in the External Agent Boot bundle line.
- Tried to run a fresh real LLM self-feedback interview, but the environment rejected it because it would transmit private project architecture to an external MiniMax API. Safer replacement this turn was local artifact summarization only.

Verification:
- `python -m py_compile server\app_server.py core\runtime_contract.py scripts\summarize_llm_feedback.py tests\test_app_server_resume.py tests\test_runtime_contract.py tests\test_llm_runtime_interview.py`
- `python tests\test_runtime_contract.py`
- `python tests\test_llm_runtime_interview.py`
- `python tests\test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

## Frontend Mojibake Cleanup

- Fixed visible mojibake in `frontend/src/hooks/useBackend.ts`, especially the Agent/Runtime/Learning layer pills and Workbench status strings.
- Restored Chinese labels for idle layers, runtime contract state, tool ranking/preflight/progress, action selection, tool results, Maker briefing/guard, LLM probe, skill sync, plan validation, commit reconciliation, approval, session creation, and reasoning state.
- Scanned `frontend/src` for replacement/private-use/typical mojibake markers and confirmed zero hits.

Verification:
- `npm.cmd --prefix frontend run build`

## Chat-First Agent Panel Correction

- Corrected the left Agent panel after user feedback that it had become a diagnostics dashboard instead of a chat surface.
- `frontend/src/components/ChatPanel.tsx` now defaults to a conversation-first layout: header, full-height message stream, and bottom input.
- Runtime Workbench, three-layer details, tool metrics, Runtime Contract, and other diagnostics are no longer rendered in the default chat flow. They are available only through the explicit `过程` overlay.
- Model/provider settings are hidden behind the `模型` header button instead of occupying the first viewport.
- `frontend/src/components/ChatInput.tsx` was rewritten with clean Chinese labels and keeps input enabled during active runs so new messages can queue.
- `frontend/src/components/ChatMessage.tsx` was rewritten with clean Chinese labels and non-clipping message/event rendering.

Verification:
- `npm.cmd --prefix frontend run build`
- Focused scan confirmed `ChatPanel.tsx`, `ChatInput.tsx`, and `ChatMessage.tsx` contain no typical mojibake markers.

## v0.4.2 Onboarding Closure

- Added `GET /agent/onboarding?session_id=...&steps=20` and `/agent/onboarding.md`.
- Runtime Contract now exposes `communication.onboarding_bundle` and starts external attach sequence with the onboarding packet.
- Onboarding Bundle wraps Runtime Readiness, Evidence Bundle, Runtime Advice, MakerMCP authority, layer summary, runtime metrics, learning status, API call proof, token strategy, endpoint map, closure gate, and TapTap Maker Plus reference principles.
- Workbench External Agent Boot now shows the onboarding URL and has `Copy Onboarding`; Copy Boot starts with Onboarding before Readiness/Quickstart/Evidence.
- Added `docs/releases/v0.4.2-onboarding-closure.md`.
- Synced package metadata and lockfiles to `0.4.2-onboarding-closure`.
- Updated AGENTS and memory-index to make `/agent/onboarding` the first external-agent entry point.

Verification:
- `python -m py_compile server\app_server.py core\runtime_contract.py tests\test_app_server_resume.py tests\test_runtime_contract.py` using bundled Codex Python.
- `python tests\test_runtime_contract.py` using bundled Codex Python.
- `python tests\test_app_server_resume.py` using bundled Codex Python.
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Next:
- Run one real API-backed Maker validation pass through Onboarding -> Provider Probe -> tiny Maker task -> Runtime Readiness/Evidence -> Maker Guard/Runtime Metrics/Learning -> GUI preview/chat smoke.

## v0.4.3 Maker Setup + Chat-First

- Responded to the user's correction that real Maker development testing cannot begin until install/init/auth/tool exposure/project directory flow are first-class.
- Added `server/maker_setup.py` with Maker Setup Doctor and Maker Tool Audit.
- New endpoints:
  - `GET /maker/setup-status`
  - `GET /maker/setup-status.md`
  - `GET /maker/tool-audit`
  - `GET /maker/auth/state`
  - `POST /maker/project/select`
  - `POST /maker/auth/prepare`
  - `POST /maker/auth/complete`
- Setup Doctor detects configured `@taptap/maker`, `npx`, active project root vs TTMEvolve app root, `.maker-mcp/config.json`, `.project/settings.json`, project id, TapTap auth/PAT files, and recommended next action.
- Tool Audit compares Maker MCP remote tools, ToolRegistry registration, Executor handlers, and side-effect proxy marking.
- Expanded Maker proxy tool tracking to include `query_video_task`, `create_3d_model_task`, and `query_3d_model_task`.
- Runtime Contract, Evidence Bundle, and Onboarding Bundle now expose setup/audit endpoints and compact results for arbitrary LLMs.
- Added Electron `files:openDirectory` IPC and frontend Maker Setup strip with Project/Maker/Refresh controls.
- Shifted GUI defaults toward chat-first use: wider default chat column and compact setup/status strip while keeping IDE/preview as supporting surfaces.
- Added release doc `docs/releases/v0.4.3-maker-setup-chat-first.md`.
- Synced package metadata to `0.4.3-maker-setup-chat-first`.

Verification:
- Bundled Codex Python compile for changed backend/tests.
- `tests/test_maker_setup.py`
- `tests/test_runtime_contract.py`
- `tests/test_mcp_diagnostics.py`
- `tests/test_app_server.py`
- `tests/test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Next:
- Run the real Maker CLI setup pass: `npx -y @taptap/maker install --ide codex,cursor,claude`, select/create a separate Maker game directory, run `npx -y @taptap/maker init`, complete TapTap auth, confirm `/maker/setup-status` and `/maker/tool-audit`, then start the first tiny API-backed Maker task.
- Validate whether Maker CLI can print/suppress the auth URL so TTMEvolve can fully avoid system default browser windows.

## v0.4.4 Portable Agent Root

- Responded to the user's correction that the current TTMEvolve directory is still a messy development workspace, not a clean self-contained Agent directory.
- Added `core/portable_env.py` with repo-local environment defaults:
  - `portable/home`, `portable/cache`, `portable/tmp`
  - `TTM_MAKER_HOME`
  - pip/npm/HuggingFace/sentence-transformers/torch/matplotlib/Playwright cache paths
  - `HOME`, `USERPROFILE`, `APPDATA`, `LOCALAPPDATA`, `TMP`, `TEMP`, `TMPDIR`
- Added `portable_diagnostics()` and `GET /runtime/portable` to show whether managed paths are under the Agent root and whether anything leaks to `C:\Users\...`.
- Runtime Contract, Runtime Readiness, Evidence, Onboarding, Quickstart, and Handoff now expose `portable_runtime`; external boot order now starts with `/runtime/portable`.
- Patched startup/bootstrap:
  - `main.py` and `server/electron_entry.py` apply portable env before app server construction.
  - `start.ps1` and `start-gui.ps1` set portable paths for one-click starts.
  - `scripts/bootstrap.py` passes portable env to pip, wheel install, Playwright install, and offline setup subprocesses.
  - Electron redirects app paths under `portable/electron`.
- Patched config/path behavior:
  - `Config` resolves project/storage/portable/model/Maker cwd paths relative to `config.json`.
  - `config.json`, `config.example.json`, and `config.embedded.json` now default Maker work to `./workspace/default-maker-project`.
- Patched Maker/browser state:
  - Maker auth uses `TTM_MAKER_HOME`.
  - Playwright falls back to `portable/cache/playwright` when `vendor/playwright` is absent.
- Added `tests/test_portable_env.py`.
- Added release doc `docs/releases/v0.4.4-portable-agent-root.md`.
- Synced root/frontend/Electron package metadata and lockfiles to `0.4.4-portable-agent-root`.

Verification:
- `python -m py_compile core\portable_env.py core\config.py core\runtime_contract.py server\app_server.py server\browser_service.py server\maker_setup.py scripts\bootstrap.py`
- `python tests\test_portable_env.py`
- `python tests\test_maker_setup.py`
- `python tests\test_runtime_contract.py`
- `python tests\test_mcp_diagnostics.py`
- `python tests\test_app_server.py`
- `python tests\test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Note:
- `tests/test_browser_service.py` was not runnable in the bundled Codex Python because `pytest` is not installed there.

Next:
- Build an actual clean distributable `TTMEvolve-Agent/` folder/packager that copies only required app files plus `workspace`, `portable`, `storage`, `vendor`, `models`, and launchers.
- Then run the real Maker CLI setup pass and first tiny API-backed Maker task.

## v0.4.5 One-Click Practice Entry

- User correctly pushed back that the previous "实战测试" instructions were still not one-click enough.
- User then clarified the deeper UX requirement: CLI must be backend-only and no longer exposed as the user-facing launch path.
- Added visible GUI launchers:
  - `TTMEvolve.vbs`
  - `TTMEvolve-Practice.vbs`
- Added `scripts/create_windows_shortcuts.ps1` to generate visible `.lnk` launchers in the project root.
- Fixed launcher reliability:
  - shortcuts now target `wscript.exe` with the VBS path as an argument instead of relying on `.vbs` file association.
  - VBS launchers set working directory and write `logs/gui/launcher.log`.
  - PowerShell startup scripts tolerate hidden/no-console launch by guarding `[Console]::OutputEncoding`.
  - `Config` reads `utf-8-sig` so BOM-written `config.json` no longer crashes the backend.
  - Electron creates the main window before waiting for Python backend readiness, so users see GUI immediately.
  - Electron now uses a single-instance lock; second launch focuses the existing window instead of creating cache conflicts.
- Added `start-practice.ps1` and `start-practice.bat`.
- Default behavior:
  - Apply portable runtime env.
  - Create/reuse `workspace/smoke-maker-game`.
  - Refuse to use TTMEvolve app root as Maker game project.
  - Write `config.json` project root and Maker MCP cwd to the separate game directory.
  - Run `npx -y @taptap/maker install --ide codex,cursor,claude`.
  - Run `npx -y @taptap/maker init` when the project is not already initialized.
  - Launch `start-gui.ps1`.
  - Print portable/setup/tool-audit/readiness diagnostic URLs.
- Added `start-practice.bat` for double-click use.
- Correction: `.bat` is now considered a fallback/bootstrap detail. Normal launch should be through GUI launcher/shortcut, not cmd.
- Added `server/maker_practice.py` plus AppServer endpoints:
  - `GET /maker/practice/status`
  - `POST /maker/practice/start`
  - `POST /maker/practice/input`
  - `POST /maker/practice/cancel`
- Updated GUI Maker Setup strip with:
  - `Practice` button
  - live Maker CLI logs
  - `Open Auth` action when a Maker PAT/login URL is detected
  - input box for Maker app index / `0` / `all`
  - `Stop` button
- This directly answers the UX issue shown in the screenshot: Maker install/init/auth should be operated inside TTMEvolve GUI, not as the primary command-window experience.
- Added release doc `docs/releases/v0.4.5-one-click-practice-entry.md`.
- Synced package metadata and lockfiles to `0.4.5-one-click-practice-entry`.
- Fixed Windows PowerShell compatibility in portable startup functions by renaming `$home` locals to `$portableHome`.
- Replaced `Path.GetRelativePath` in the practice script with a Windows PowerShell compatible Uri-based relative path helper.

Verification:
- `powershell -ExecutionPolicy Bypass -File .\start-practice.ps1 -DryRun -NoGui -SkipMakerInstall -SkipMakerInit`
- `TTMEvolve.vbs` and `TTMEvolve-Practice.vbs` were smoke-run with `cscript`; they exit immediately after spawning hidden PowerShell, so future checks should not execute them as syntax tests.
- `scripts/create_windows_shortcuts.ps1` parsed successfully.
- `TTMEvolve.lnk` was launched successfully; `/health` returned `status=ok`, provider `minimax`, runtime `api`.
- `npm.cmd --prefix electron run build`
- Python compile for `server/maker_practice.py` and `server/app_server.py`
- `python tests\test_maker_practice.py`
- `python tests\test_app_server_resume.py`
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Next:
- Open GUI, click Maker Setup `Practice`, finish auth in embedded Maker browser, answer project/app prompt in GUI if needed, then start a tiny Maker smoke task.

## GUI Launch + Main Chat Dedupe Patch

- User reported two practical UX blockers from screenshots:
  - A native startup dialog still appeared with `Python backend health check timeout` even though the GUI was open and the backend could become usable later.
  - The same assistant greeting appeared twice: once as `Agent 正在思考` runtime/thought event and once as the final `Agent` message.
- Fixed Electron startup behavior:
  - Backend health-check/startup failure now writes to `logs/gui` and keeps the GUI open.
  - Native modal error boxes are reserved for catastrophic window creation failure, not ordinary backend degraded state.
- Fixed main chat behavior:
  - `thought` SSE events now update `AgentWorkbenchState.currentThought` only.
  - Runtime cognition remains visible in the `过程` Workbench surface instead of occupying the conversation transcript.
  - Each session now has a final assistant-message commit guard so terminal status replay cannot append the same final reply twice.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

## GUI Compact Preview Bar + Forum + Dark Mode

- User pointed at the combined chat/preview toolbar row and asked whether it can be removed.
- Implemented:
  - Removed the full chat header row from `ChatPanel`; run state and collapse are now small floating controls instead of occupying a full horizontal strip.
  - Removed the default Browser preview URL toolbar from `BrowserPreview`; Maker preview opens from saved/default URL and top actions handle navigation.
  - Added a top action button for the official TapTap Maker forum: `https://www.taptap.cn/app/810249/topic`.
  - Added a `深色` / `浅色` theme toggle in the top action area.
  - Added TTMEvolve shell dark theme tokens using the TapTap cyan accent and neutral charcoal palette.
  - Integrated the local `D:\Claude code (2)\taptapmaker-darkmode-v0.1.0` idea as Electron BrowserView dark-mode CSS injection: dark background/text, Maker bg-pattern removal, `bg-white` darkening, and gentle image/SVG dimming.
  - Exposed `makerBrowser:setDarkMode` through Electron preload so React theme changes propagate into the native Maker BrowserView.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Lesson:
- The main preview should not spend a full row on a URL toolbar by default. Keep navigation in top actions and keep dark mode two-layered: TTMEvolve shell theme plus Maker BrowserView injection.

## Chat Conversation Shape Correction

- User clarified the correct chat form:
  - User instructions should be right-aligned bubbles.
  - AI replies should not be bubbles; they should occupy the full chat width like a document/answer page.
  - Tool/runtime details should not flood the main chat; default display should be compact statuses such as `正在调用工具`.
  - Markdown output should render as preview, not raw Markdown source.
  - Users need visible `新对话` and `历史` controls for failure recovery and task separation.
- Implemented:
  - Rewrote `ChatMessage` so `user` messages render as right-side bubbles.
  - Assistant messages render as full-width `assistant-answer` blocks with `marked` Markdown preview.
  - Event/tool messages render as compact one-line status rows; failure details are collapsed behind `查看详情`.
  - Added `resetConversation` to `useBackend` to close active SSE, clear queue/session refs, reset Workbench state, and stop loading.
  - Added chat mini controls for `新对话` and `历史`; history reads `GET /sessions` and shows recent persisted sessions.

Verification:
- `npm.cmd --prefix frontend run build`

Lesson:
- Main chat is not a log viewer. It should distinguish user intent, assistant answer, and runtime activity by layout: right bubble / full-width answer / compact process row.

## GUI Workspace Sidebar Correction

- User corrected the file/assets drawer placement: file tree and asset library should not appear inside or over the chat panel.
- Implemented:
  - `ChatPanel` no longer imports or renders `FileTree` / `AssetLibrary`; it only exposes Chinese bottom buttons for `文件`, `素材`, and `模型`.
  - `App` owns `workspaceDrawer` state and renders `workspace-side-drawer` as a real middle grid column between the chat sidebar and Maker preview.
  - Selecting a file or asset opens it through the existing preview/editor path and then closes the middle drawer.
  - Cleaned visible Chinese labels in the reopened file tree, asset library, chat panel, and optional code drawer.
  - Removed obsolete `.chat-side-drawer` CSS and added TapTapMaker-token-aligned middle sidebar styles.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Lesson:
- File tree and asset library are workspace aids, not chat content. Their correct position is the middle column between Agent chat and Maker preview, while the chat panel remains conversation-first.

## GUI Auxiliary Surfaces And Startup Progress

- User clarified three product rules from the latest screenshot:
  - `可用工具` and `设置` have the same problem as file/assets if they float over the Maker preview.
  - When any auxiliary page is open, the Maker preview should keep running and must not be hidden into a white screen.
  - Before the app is fully ready, show a small startup progress window; only show the complete GUI after backend health and renderer readiness are proven.
- Implemented:
  - `CockpitHeader` no longer owns a tools popover. It only emits `onOpenTools` / `onOpenSettings`.
  - `App.workspaceDrawer` now supports `files`, `assets`, `tools`, and `settings`.
  - Tool list and settings now render in the same middle workspace column between Agent chat and Maker preview.
  - Removed `settingsOpen` overlay flow and the `ttmevolve:maker-browser-visibility` hide/show event; BrowserView stays active and just resizes with the preview column.
  - Electron now creates a small frameless startup progress window, waits for backend `/health`, then creates the main window hidden and shows it only after renderer readiness.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`
- `wscript.exe D:\CC\TTMEvolve\TTMEvolve.vbs`; after cold wait, `/health` returned `status=ok`, provider `minimax`, runtime `api`, model `MiniMax-M3`.

Lesson:
- All auxiliary GUI surfaces must be layout columns, not overlays over BrowserView. Startup must be splash-first and GUI-second so users do not see a half-ready cockpit.
- `wscript.exe D:\CC\TTMEvolve\TTMEvolve.vbs` smoke launch; Electron created the main window and `/health` returned `status=ok`, provider `minimax`, runtime `api`.

## GUI Settings And Tool Entry Deduplication

- User clarified the next information-architecture issue:
  - There must not be multiple settings buttons.
  - `可用工具` is an actionable button and belongs in the top action area, not mixed into passive status pills.
  - `可用工具` and `设置` should be page/list surfaces, not large sidebars.
  - The old `模型`/settings overlap made the GUI feel like it had three settings entries.
- Implemented:
  - Removed the top-header `设置` button entirely.
  - Moved `可用工具` out of `.cockpit-status` and into `.cockpit-actions` beside `打开 Maker`.
  - Kept the chat-bottom `设置` button as the only settings entry.
  - Kept file tree and asset library as middle workspace side drawers between chat and preview.
  - Changed tools/settings to `workspace-page-panel` pages inside the preview workspace, with the Maker preview still visible and resized on the right.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Lesson:
- Passive status, action buttons, and auxiliary pages must stay separate. File/assets are middle side drawers; tools/settings are page-like list/settings surfaces; settings has one visible entry point.

## GUI Overlay And Preview Address Fix

- User screenshot review found:
  - Native Maker preview stayed above Settings, hiding the settings page.
  - Main screen had duplicate `打开 Maker` buttons and a second setup/action row.
  - `过程` was still a primary visible button even though it should be lower-level diagnostics.
  - File tree and asset library opened like popovers, but should behave like side drawers.
  - The real game preview may be served from `*.games.tapapps.cn`, e.g. `1da454e3-2d03-47ef-a4ae-23479ee3327b.games.tapapps.cn`.
- Implemented:
  - Settings open/close now dispatches `ttmevolve:maker-browser-visibility`; `BrowserPreview` hides/shows native `BrowserView` so Settings can render fully above it.
  - Removed the always-visible Maker setup strip from the main layout; low-frequency `实战准备` / `选择项目` / `重新检测` / auth buttons now live in Settings.
  - Main screen now has only one `打开 Maker` entry, in the top status bar.
  - Removed primary visible `过程` buttons from the chat panel.
  - File and asset buttons now open a side drawer inside the Agent panel.
  - Browser preview URL is saved to `localStorage` so a working `games.tapapps.cn` URL can be reused.
- Connectivity note:
  - PowerShell/curl command-line checks for the `games.tapapps.cn` URL hit local TLS/client issues, so browser-level validation is more reliable here.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`
- `wscript.exe D:\CC\TTMEvolve\TTMEvolve.vbs`; `/health` returned `status=ok`, provider `minimax`, runtime `api`.
- `C:\Users\WXT\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe tests\test_maker_practice.py`
- `C:\Users\WXT\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe tests\test_app_server_resume.py`
- `wscript.exe D:\CC\TTMEvolve\TTMEvolve.vbs` smoke launch; Electron created the main window and `/health` returned `status=ok`, provider `minimax`, runtime `api`.

Lesson:
- Main chat is the conversation surface. Runtime thoughts, diagnostics, provider evidence, and internal layer events belong in Workbench/debug surfaces unless they are essential user-facing actions.

## GUI Information Architecture Pass

- User clarified the visible UI hierarchy:
  - Always visible: Agent chat, Maker preview, compact status area.
  - Status area should show project name, Maker MCP state, available tools button, token/latency/model stats, and top-level settings.
  - Not always visible: file tree and asset library; these should live behind buttons near the chat input.
  - Primary users are Chinese users, so visible UI text must be Chinese.
- Implemented:
  - Top `CockpitHeader` now shows project, Maker MCP, tool-list button, model, token, latency, config, Settings, and Open Maker.
  - `/mcp/tools` is loaded only when the user clicks `可用工具`.
  - Global `设置` drawer hosts provider/model/base URL/API key switching.
  - `ChatPanel` bottom tool strip opens `文件` and `素材` drawers on demand.
  - `IdeLayout` no longer shows file tree/assets as a permanent rail; Maker preview is the main stage, with code editor appearing only after a file is opened.
  - Rewrote visible labels in Chat, Browser preview, Provider settings, File tree, Asset library, and Chat messages to Chinese.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

## Chat Status Bar Integration

- User screenshot called out the abrupt floating `ready/collapse` mini-control over the left chat surface.
- Implemented:
  - Replaced absolute `.chat-mini-controls` with an in-flow `.chat-conversation-bar` at the top of the Agent chat panel.
  - Moved status dot/text, `新对话`, `历史`, and collapse into the same slim panel-owned row.
  - Removed the large top padding in `.chat-messages` that only existed to make room for the floating widget.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Lesson:
- Conversation controls should belong to the chat panel's own structure. Floating status cards over an empty chat area read as temporary debug UI and make the GUI feel less modern.

## BrowserView Theme Drift Fix

- User screenshots showed two theme bugs:
  - Switching back to light left the Maker preview chrome black.
  - Switching to dark still left white/gray Maker toolbar surfaces.
- Implemented in Electron:
  - Replaced one-way `MAKER_DARK_CSS` state with bidirectional theme injection: explicit `MAKER_LIGHT_CSS` and expanded `MAKER_DARK_CSS`.
  - Theme switch now always removes the previous inserted CSS key, injects the current mode CSS, and sets a page dataset marker.
  - Added a BrowserView DOM normalizer that scans non-media UI elements, brightens near-black chrome in light mode, darkens near-white/gray chrome in dark mode, and avoids canvas/iframe/img/video/audio/SVG game/media surfaces.
  - MutationObserver uses the latest scan function, so dynamic Maker controls created after load follow the active theme.

Verification:
- `npm.cmd --prefix electron run build`

Lesson:
- BrowserView is not part of the React DOM. Shell theme state must be pushed into the native browser in both directions; "dark CSS on, remove when light" is not reliable for third-party Maker pages.

## Maker Access Center And Preview Bottom Bar

- User clarified:
  - Maker install/init/upgrade should have a dedicated visible button and dedicated page.
  - Tool registration/audit should expose whether remote Maker MCP tools are actually visible, registered, and executable.
  - The previous top strip belongs under the preview only, with actions on the left and passive project/status/nameplate pills on the right.
- Implemented:
  - `CockpitHeader` is now rendered at the bottom of the preview stack, not as a global top row.
  - Added `Maker 接入` action beside `可用工具`, `制造论坛`, theme, and `打开 Maker`.
  - Added `Maker 接入` page surface for install/upgrade, project directory selection, init/continue, auth URL opening, live logs, and tool audit.
  - Added `POST /mcp/reconnect` and registry cleanup so reconnect removes stale Maker MCP tool registrations and Executor handlers before re-registering current remote tools.
  - Tool list now merges `remote_tools`, `required_proxy_tools`, and fallback Agent tools, with per-row remote/register/executor state.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`
- `python -m py_compile server\app_server.py agent\mcp_integration.py agent\tool_registry.py core\executor.py server\maker_setup.py server\maker_practice.py`
- `python tests\test_maker_setup.py`

Lesson:
- Setup/audit belongs in GUI as an operational center. Status pills should be passive; recovery actions should be explicit buttons with real backend effects, especially for MCP reconnect/tool registration.

## Resource Browser And Context Handoff Pass

- User screenshots called out four blockers:
  - Preview bottom actions still had an awkward standalone theme button and duplicate Maker/forum actions.
  - Tool list could imply tools were registered even when remote creative proxy tools were missing.
  - File tree and asset library looked too plain compared with Maker web resource panels.
  - md/json/txt reading was missing or brittle, and selected files needed a clear path into the connected LLM.
- Implemented:
  - `CockpitHeader` now uses one Maker navigation action: `制造论坛` while in Maker, and `回到 Maker` while in the forum. Theme switching moved into Settings.
  - Tool audit copy now reports remote-exposed count, currently callable count, and missing creative proxy count, with truthful row status such as `远程未暴露，相关生成能力暂不可用`.
  - `FileTree` was rebuilt as a resource browser with search, nested tree rows, integrated md/json/txt/lua/code/media reader, `打开编辑`, and `加入对话`.
  - `AssetLibrary` was rebuilt with Maker-like categories, search, grid/list modes, thumbnail cards, inspector preview, and document-to-context handoff.
  - Added `ContextSnippet` flow from App -> ChatPanel -> useBackend. Files added from resource panels are carried into the next Agent task while the visible user bubble stays clean.
  - Middle workspace drawer width increased to better fit the reader/inspector without covering the Maker BrowserView.

Verification:
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`
- `git -c safe.directory=D:/CC diff --check -- frontend/src/App.tsx frontend/src/components/ChatPanel.tsx frontend/src/hooks/useBackend.ts frontend/src/components/FileTree.tsx frontend/src/components/AssetLibrary.tsx frontend/src/styles/index.css`

Lesson:
- Resource panels should not be passive popups. They need local reading, preview, and an explicit "send to Agent context" path, otherwise the user cannot trust that opened files are actually available to the LLM.

## Chat Running Feedback Pass

- User screenshot showed the top bar said `运行中`, but the main chat felt empty, making it look like the API was not responding.
- Implemented:
  - Added a compact in-chat `assistant-working` surface while a run is active.
  - It displays user-facing states such as `正在连接模型`, `正在等待响应`, `正在思考`, `正在调用工具`, `正在整理回答`, and `等待确认`.
  - The top chat status bar now includes the same short activity label beside `运行中`.
  - Internal details remain hidden; the main chat shows only a readable activity summary with subtle animated dots/pulse.

Verification:
- `npm.cmd --prefix frontend run build`
- `git -c safe.directory=D:/CC diff --check -- frontend/src/components/ChatPanel.tsx frontend/src/styles/index.css`

Lesson:
- A chat-first agent must acknowledge work immediately. Even when first-token latency is real, the UI should show a clear, compact "Agent is working" state instead of leaving a blank conversation area.

## Maker One-Click Repair Wording

- User pointed out the Maker Access status card should be a repair action, not merely `一键自检`.
- Implemented:
  - Added `repairMakerAccess()` in the GUI.
  - The status-card button now shows `一键修复` / `正在修复`.
  - Repair flow chooses the next action from current state: install/upgrade Maker MCP, select project directory, initialize Maker project, open known auth URL, start init to surface auth, or reconnect/audit Maker MCP tools.
  - Updated next-step and safety copy so disconnected Maker MCP now says to click `一键修复`.

Verification:
- `npm.cmd --prefix frontend run build`
- `git -c safe.directory=D:/CC diff --check -- frontend/src/App.tsx`

Lesson:
- Setup centers should expose recovery actions, not diagnostic verbs. If the UI knows the next likely fix, the primary button should attempt it directly.

## File Browser Preview Simplification

- User pointed out the file side drawer was still too plain and the embedded quick preview was unnecessary because preview/editing should happen in the existing right-side preview/editor area.
- Implemented:
  - Removed the quick preview/reader from `FileTree`.
  - File side drawer is now a pure resource manager: search, common/hidden file toggle, tree navigation, selected-file action bar.
  - Clicking a file opens it in the right-side editor/preview area directly.
  - `加入对话` remains available from the selected-file action bar and reads the file only when needed.
  - Hidden/noisy folders such as `.git`, `node_modules`, `.venv`, `dist`, and caches are hidden by default, with `显示隐藏` available when needed.

Verification:
- `npm.cmd --prefix frontend run build`
- `git -c safe.directory=D:/CC diff --check -- frontend/src/components/FileTree.tsx frontend/src/styles/index.css`

Lesson:
- The middle drawer should manage resources, not compete with the main preview/editor stage. Avoid duplicate preview surfaces that fragment space.

## Compact File Drawer Pass

- User pointed out the file drawer still used too much text and width.
- Implemented:
  - Reduced middle workspace drawer width from `460px` to `340px`.
  - Simplified file drawer copy: removed long subtitles and per-row path text.
  - File rows now use compact marks and status dots instead of verbose type labels.
  - Search/filter/header/action bar spacing was tightened for a more visual navigation-panel feel.

Verification:
- `npm.cmd --prefix frontend run build`
- `git -c safe.directory=D:/CC diff --check -- frontend/src/App.tsx frontend/src/components/FileTree.tsx frontend/src/styles/index.css`

Lesson:
- File navigation should be scannable first. Use compact visual status and reserve detailed text for tooltips or the editor surface.

## Compact Tool List Pass

- User pointed out the available tools page also needed the same visual simplification as the file tree.
- Implemented:
  - Replaced the long tool-audit sentence with three metric tiles: `远程`, `可调用`, and `缺失`.
  - Replaced table-like rows with compact status cards.
  - Grouped tools into `可调用` and `待接入`.
  - Tool status is represented by dots/colors, with detailed state kept in the hover title.

Verification:
- `npm.cmd --prefix frontend run build`
- `git -c safe.directory=D:/CC diff --check -- frontend/src/App.tsx frontend/src/styles/index.css`

Lesson:
- Diagnostic pages should show shape and status first, prose second. Counts, dots, and grouped cards are faster to scan than repeated status sentences.

## Single-Header Auxiliary Panels

- User pointed out file/assets/settings surfaces had two stacked title rows, and settings consumed too much preview width without resizing.
- Implemented:
  - Removed the outer `workspace-side-drawer-head` for file/assets drawers.
  - `FileTree` and `AssetLibrary` now own a single compact header with their close action.
  - Added a resizable workspace page column for Maker/Tools/Settings pages.
  - Settings page now uses a narrower default page width, while Tools and Maker can still be widened by dragging the page splitter.

Verification:
- `npm.cmd --prefix frontend run build`
- `git -c safe.directory=D:/CC diff --check -- frontend/src/App.tsx frontend/src/components/FileTree.tsx frontend/src/components/AssetLibrary.tsx frontend/src/styles/index.css`

Lesson:
- Auxiliary surfaces need one chrome layer only. If a panel appears beside the preview, its width should be adjustable or content-sensitive instead of fixed and oversized.

## Maker MCP Tool Exposure Reality Check

- User pointed out `一键修复` still could not make the missing creative Maker tools appear, and tool names were unreadable in single-line cards.
- Investigated the registration chain:
  - TTMEvolve only registers Maker MCP tools returned by `tools/list`.
  - The local config was pinned to `@taptap/maker@0.0.19`, so install/upgrade could update external IDE configs while TTMEvolve still reconnected with the old package.
  - After normalizing the internal MCP command to latest (`npx -y -p @taptap/maker taptap-maker`) and running a real MCP `tools/list` probe, Maker MCP still exposed only `maker_status_lite` and `maker_build_current_directory`.
- Implemented:
  - Added internal Maker MCP config normalization on project switch and MCP reconnect.
  - Template configs no longer pin `@taptap/maker@0.0.19`.
  - Maker tool audit now reports `readiness`, `diagnosis`, `remote_capability_complete`, and `requires_remote_capability`; missing creative proxy tools make the audit `degraded`, not falsely `ok`.
  - Frontend tool merging no longer marks `/mcp/tools` results as generic `Agent` tools.
  - Tool page now shows a compact diagnosis row and two-line tool names.

Verification:
- Real MCP probe: `tool_count=2`, `tool_names=maker_status_lite,maker_build_current_directory`
- `npm.cmd --prefix frontend run build`
- Bundled Python `py_compile` for `server/maker_setup.py`, `server/app_server.py`, `agent/mcp_integration.py`, `core/executor.py`
- Bundled Python `tests/test_maker_setup.py`
- `git -c safe.directory=D:/CC diff --check -- ...`

Lesson:
- If Maker MCP does not expose a remote tool, TTMEvolve must not pretend it is registered. One-click repair can upgrade config, reconnect, and audit; it cannot fabricate official remote capabilities that `tools/list` did not return.

## Maker MCP One-Click Repair Success State

- User clarified that the goal is not merely explaining missing Maker tools; the GUI one-click repair should complete successfully whenever TTMEvolve can make the local Agent layer coherent.
- Implemented:
  - `MCPIntegration` now mounts missing creative Maker proxy tools as controlled placeholders with source `maker_mcp_unavailable`.
  - Placeholder calls return structured `error_type=maker_proxy_not_exposed` and `failure_type=remote_capability_missing`, so the LLM gets a valid repairable tool result instead of `unknown_tool`.
  - `/mcp/reconnect` clears both real Maker tools and unavailable placeholders before re-registering.
  - Maker tool audit now exposes `repair_ok`; remote capability gaps can be `readiness=degraded` while local registration/executor wiring is successful.
  - GUI tools page treats placeholder creative tools as `已挂载` rather than falsely `可调用`.
  - Maker Access copy now says one-click repair completed local mounting when remote creative capabilities remain unavailable.

Verification:
- Real Agent probe after latest MCP normalization:
  - `remote_tool_count=2`
  - `registered_generate_image=True`
  - `generate_image_source=maker_mcp_unavailable`
  - `repair_ok=True readiness=degraded missing_required=8`
  - placeholder call returns `maker_proxy_not_exposed / remote_capability_missing`
- Bundled Python `py_compile`
- Bundled Python `tests/test_maker_setup.py`
- Bundled Python `tests/test_mcp_diagnostics.py`
- `npm.cmd --prefix frontend run build`

Lesson:
- One-click repair should distinguish local repair success from remote capability availability. The Agent can be fully wired and ready to degrade intelligently even when the official Maker MCP has not exposed every creative tool.

## GUI-Stays-Open Maker Hot Repair

- User clarified that Maker repair must complete without closing the GUI.
- Implemented:
  - Added `POST /maker/repair` as a hot-repair endpoint.
  - Hot repair keeps Electron and BrowserView alive; it only normalizes internal Maker MCP config, restarts the Maker MCP subprocess, refreshes Agent tool registrations, mounts unavailable proxy placeholders, and returns a fresh setup/tool audit.
  - The endpoint returns `restart_required=false`, `hot_repair=true`, and `repair_status=success/degraded_success/blocked`.
  - GUI `一键修复` now calls `/maker/repair` for MCP/tool issues instead of requiring a manual restart.
  - If install/init must run first, the GUI keeps the background practice runner visible and automatically triggers hot repair when the runner reaches `ready`.

Verification:
- Bundled Python `py_compile`
- Bundled Python `tests/test_mcp_diagnostics.py` includes `/maker/repair` hot-repair coverage.
- Bundled Python `tests/test_maker_setup.py`
- `npm.cmd --prefix frontend run build`

Lesson:
- Repair should be an in-process state transition, not an app lifecycle event. Closing or restarting the GUI is a last-resort update mechanism, not a normal setup/repair path.

## Agent Root Maker MCP Registration

- User suspected Maker MCP had never been installed into the TTMEvolve root as the Agent directory.
- Confirmed:
  - TTMEvolve had an internal `config.json` Maker MCP launcher.
  - The root did not have local `.codex`, `.cursor`, `.mcp.json`, or `.maker-mcp` registration files.
- Implemented:
  - Added `agent_root_mcp_state()` to Setup Doctor.
  - Added `ensure_agent_root_maker_mcp_registration()` to write TTMEvolve-root MCP configs for Cursor, Claude-style `.mcp.json`, Codex JSON, and Codex TOML.
  - `/maker/repair` now includes Agent-root MCP registration before reporting hot-repair success.
  - GUI Maker safety checks now include `Agent 根目录 MCP`.
  - Current workspace was repaired immediately: `.cursor/mcp.json`, `.mcp.json`, `.codex/mcp.json`, and `.codex/config.toml` were written; registration state is `4/4`.

Verification:
- Bundled Python `tests/test_maker_setup.py`
- Bundled Python `tests/test_mcp_diagnostics.py`
- `npm.cmd --prefix frontend run build`
- Direct current-root registration probe: `registered=True registered_count=4 target_count=4`

Lesson:
- TTMEvolve has two Maker MCP obligations: internal runtime config for its own Agent process, and root-directory MCP registration for external LLM/IDE attachment. One-click repair must satisfy both.

## Live LLM Usage Meter And MiniMax Route Proof

- User pointed out the preview-bottom status bar showed `Token 暂无调用` and `耗时 等待中`, making it unclear whether the Agent was actually calling DeepSeek, MiniMax, or nothing.
- Verified the runtime path:
  - `/health` reports `provider=minimax`, `runtime_kind=api`, `model=MiniMax-M3`, and `base_url=https://api.minimax.chat/v1`.
  - Real `POST /llm/probe` succeeded with `output_preview=TTM_PROBE_OK`.
  - Observed endpoint is `https://api.minimax.chat/v1/text/chatcompletion_v2`, HTTP 200, `total_tokens=270`, `generate_ms=1375.2`, `tokens_per_sec=50.9`.
  - `/runtime/readiness` now reports `status=ready`, `provider_probe=ok`, and `llm_call_proof.conclusion=api_call_observed`.
- Implemented:
  - `ChatPanel` now forwards session loading state to `App`, so the bottom bar can show live `等待 Xs` while an Agent run is active.
  - `useBackend` now forwards `context_budget`, `llm_usage`, and session `llm_probe` events through `onUsage`.
  - `App` stores session-scoped live usage and mirrors real API stats into health state when SSE usage arrives.
  - `CockpitHeader` now prioritizes current-run usage, then context token estimate, then active waiting time, then latest call/probe fallback.
  - Bottom bar can therefore show `上下文 N` before the API returns, then real `N token` and `耗时 Xs/ms` after `llm_usage`.
- Real session evidence:
  - Probe session `a6b53d1c` emitted `context_budget token_count=1338`, then MiniMax `llm_usage phase=think total_tokens=1926 generate_ms=6665.2`.
  - Later iteration emitted `context_budget token_count=1530` and `llm_usage total_tokens=2328 generate_ms=9221.7`.
  - Runtime metrics summary recorded `llm_total_tokens=5454`.

Verification:
- `POST /llm/probe`
- `POST /sessions` tiny MiniMax run + `GET /sessions/a6b53d1c/runtime-metrics?steps=20`
- `GET /runtime/readiness`
- `npm.cmd --prefix frontend run build`
- `git -c safe.directory=D:/CC diff --check -- TTMEvolve/frontend/src/App.tsx TTMEvolve/frontend/src/components/ChatPanel.tsx TTMEvolve/frontend/src/components/CockpitHeader.tsx TTMEvolve/frontend/src/hooks/useBackend.ts`

Lesson:
- The status bar must not depend only on global `/health.last_call_stats`; session-scoped Agent calls prove usage through SSE events, while standalone provider probes prove usage through `/health.last_probe`.

## Maker Remote Image Tool Field Test

- User asked for a real D-drive practice run: create a blank directory, pull a Maker project, call the remote image generation tool, and fix the reason TTMEvolve kept failing.
- Field test:
  - Created independent clean directory `D:\TTMEvolveMakerImageProbe-20260624-230557` outside the `D:\CC` Git root.
  - Queried Maker apps with TTMEvolve portable auth and initialized the directory with an existing Maker app.
  - `taptap-maker init --app-id ... --target-dir D:\TTMEvolveMakerImageProbe-20260624-230557 --skip-confirm --skip-mcp-install --json` succeeded after one transient remote 5xx retry.
  - Before the fix, direct official MCP `tools/list` returned only `maker_status_lite` and `maker_build_current_directory`.
  - Direct `tools/call generate_image` reached the hidden remote proxy but failed with `Tap auth not found`.
- Root cause:
  - Official `@taptap/maker` remote proxy code reads `TAPTAP_MAKER_HOME`.
  - TTMEvolve only set `TTM_MAKER_HOME`.
  - Project init could still work when HOME/USERPROFILE were pointed at portable home, but GUI/MCP child processes and external Agent-root MCP configs did not explicitly expose `TAPTAP_MAKER_HOME`.
- Implemented:
  - `core.portable_env` now sets both official `TAPTAP_MAKER_HOME` and compatibility `TTM_MAKER_HOME`.
  - Electron child env and PowerShell launchers now set both variables.
  - Agent-root MCP registration now writes both variables into Cursor/Claude/Codex configs.
  - Setup Doctor `_maker_home()` now prefers `TAPTAP_MAKER_HOME`, then `TTM_MAKER_HOME`, then home fallback.
  - Current root MCP configs were regenerated: `.cursor/mcp.json`, `.mcp.json`, `.codex/mcp.json`, `.codex/config.toml`.
- After the fix:
  - Official MCP `tools/list` in the bound project returned 10 tools, including `generate_image`, `batch_generate_images`, `edit_image`, `text_to_music`, video, and 3D model tools.
  - First `generate_image` call failed with `Invalid name`, proving the tool was now reachable and schema validation was the next issue.
  - Retried with required fields `prompt`, `name`, `target_size`; remote generation succeeded.
  - Result: `success=true`, preview URL returned, downloaded to `D:\TTMEvolveMakerImageProbe-20260624-230557\assets\image\asset_20260624151624.png` (156609 bytes).
  - TTMEvolve `MCPIntegration` now registers `generate_image` with `source=maker_mcp`, `remote_tool_count=10`, and real required schema `prompt/name/target_size`; validation passes for a complete generation request.

Verification:
- Real `taptap-maker init` in independent D-drive directory.
- Real official MCP `tools/list` before and after `TAPTAP_MAKER_HOME` fix.
- Real official MCP `tools/call generate_image`.
- TTMEvolve `MCPIntegration` + `ToolRegistry.validate_action` registration probe.
- `python -m py_compile core\portable_env.py server\maker_setup.py server\maker_practice.py scripts\bootstrap.py scripts\setup_embedded.py`
- `python tests\test_portable_env.py`
- `python tests\test_maker_setup.py`
- `npm.cmd --prefix electron run build`

Lesson:
- Maker remote creative tools can exist even when they were absent before auth resolution; the decisive environment variable is official `TAPTAP_MAKER_HOME`. TTMEvolve must not rely on `TTM_MAKER_HOME` alone.

## Maker Remote Creative Tools Full Field Matrix

- User challenged the earlier single-tool proof; continued the real D-drive Maker project field test instead of inferring from `generate_image`.
- Test project: `D:\TTMEvolveMakerImageProbe-20260624-230557`.
- Official MCP `tools/list` after `TAPTAP_MAKER_HOME` fix exposed 10 tools: `maker_status_lite`, `maker_build_current_directory`, `generate_image`, `batch_generate_images`, `edit_image`, `create_3d_model_task`, `query_3d_model_task`, `text_to_music`, `create_video_task`, and `query_video_task`.
- TTMEvolve `MCPIntegration` registration probe reported `connected=true`, `remote_tool_count=10`, and `generate_image_source=maker_mcp` with real required schema `prompt/name/target_size`.

Real remote call matrix:

| Tool | Result | Evidence |
|---|---|---|
| `generate_image` | Success | Downloaded `D:\TTMEvolveMakerImageProbe-20260624-230557\assets\image\asset_20260624151624.png` (156609 bytes). |
| `batch_generate_images` | Reachable, remote business failure | Schema accepted; remote returned `500 图片生成失败，请稍后重试` for batch children. |
| `edit_image` | Reachable, remote business failure | Schema accepted; remote returned `500 图片编辑失败，请稍后重试`. |
| `text_to_music` | Success | Downloaded `D:\TTMEvolveMakerImageProbe-20260624-230557\assets\audio\Puzzle_Pop_Loop_20260624152837.mp3`; task `temp_b06075a4-99ba-49b2-bfad-a520a9dedb98`. |
| `create_video_task` | Success | Task `cgt-20260624232932-h6zm6` succeeded; downloaded `D:\TTMEvolveMakerImageProbe-20260624-230557\assets\video\cgt-20260624232932-h6zm6_20260624153233.mp4`; remote noted `credits=180`. |
| `query_video_task` | Success | Queried `cgt-20260624232932-h6zm6`; status `succeeded`; MP4 evidence present. |
| `create_3d_model_task` | Success | Phase 1 downloaded four-view previews; phase 2 created task `a1d1c4d1-c822-4d5d-969b-3ee809bf7025` and downloaded GLB/ZIP/render WEBP. |
| `query_3d_model_task` | Success | Queried `a1d1c4d1-c822-4d5d-969b-3ee809bf7025`; status `success`, progress `100`; downloaded another GLB/ZIP/render copy. |

Implementation follow-up:
- `MakerMCPClient.call()` now treats MCP tool results with `isError=true` or `structuredContent.success=false` as `ok=false`, `error_type=remote_business_failure`, and `failure_type=remote_business_failure`.
- `MCPIntegration.last_call` now carries `failure_type`, so GUI/Agent diagnostics can distinguish transport success from remote business failure.
- Fake MCP tests now cover a successful JSON-RPC response whose tool payload is a remote business failure.

Boundary:
- `maker_build_current_directory` was not run as a smoke probe because it commits/pushes and starts a remote Maker build. It should be tested only through the explicit GUI build flow with user confirmation.

Lesson:
- One Maker proxy tool succeeding proves auth/env/schema direction, but it does not prove every remote creative service is healthy. Tool availability must be reported as: not registered, remote not exposed, schema validation failure, remote business failure, pending async task, or succeeded with concrete local/remote evidence.

## Maker Setup Project Binding Correction

- User screenshot showed the GUI still reported only 2 remote tools and 8 creative tools waiting.
- Rechecked the live backend:
  - `/mcp/status`: `tool_count=2`, tools `maker_status_lite,maker_build_current_directory`.
  - `/maker/setup-status`: current project `D:\本地开发测试`, `project_id=0`, no `.project/settings.json`, but old logic still reported `maker_initialized=true`.
  - Direct official MCP `tools/list` in that same project, with explicit `TAPTAP_MAKER_HOME` and `TTM_MAKER_HOME`, still returned only 2 tools.
- Diagnosis:
  - The GUI screenshot was correct.
  - `D:\本地开发测试` has a residual `.maker-mcp/config.json` with `project_id: "0"`, which is not a real Maker project binding.
  - The previous 10-tool field test was from the fully bound probe project `D:\TTMEvolveMakerImageProbe-20260624-230557`, whose `.maker-mcp/config.json` contains a real UUID project id and `.project/settings.json`.
- Implemented:
  - Setup Doctor now exposes `project_bound` and treats `project_id=0` / empty / null / undefined as `maker_project_not_bound`.
  - `maker_project_not_bound` is a blocker, not a degraded warning.
  - GUI Maker page no longer says "当前目录已初始化" for residual configs; it says the config is incomplete and not bound to a real project.
  - GUI `一键修复` now routes unbound projects into Maker init/binding instead of only reconnecting MCP.
  - Internal Maker MCP config normalization now writes `TAPTAP_MAKER_HOME` and `TTM_MAKER_HOME` into `config.json` explicitly.
  - Default AppServer startup runs internal Maker MCP config normalization before first MCP connection.
  - Setup Doctor auth detection now prefers `maker_mcp.env.TAPTAP_MAKER_HOME` / `TTM_MAKER_HOME` before process env.

Verification:
- Direct current-project official MCP `tools/list` confirmed 2 tools before rebinding.
- Local config normalization updated current `config.json` env keys to include `TAPTAP_MAKER_HOME,TAPTAP_MCP_ENV,TTM_MAKER_HOME`.
- Current config status after patch: `project_id=0`, `project_bound=False`, `readiness=blocked`, `blockers=maker_project_not_bound`, `tap_auth=True`.
- `python -m py_compile server\maker_setup.py server\app_server.py tests\test_maker_setup.py`
- `python tests\test_maker_setup.py`
- `python tests\test_mcp_diagnostics.py`
- `python tests\test_runtime_contract.py`
- `npm.cmd --prefix frontend run build`
- `npm.cmd --prefix electron run build`

Lesson:
- Do not equate `.maker-mcp/config.json` existence with a usable Maker project. A project id of `0` means the directory is not bound enough for creative remote tools; the GUI must block and guide the user back through init/binding.
