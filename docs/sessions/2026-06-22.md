# 2026-06-22 Session Summary

Focused on the user's concern that TTMEvolve still feels below mature agent IDE quality.

Delivered:
- Added resizable/collapsible Agent sidebar in `frontend/src/App.tsx`.
- Rebuilt `frontend/src/components/IdeLayout.tsx` with persistent file/assets and preview pane resizing/collapse.
- Added preview collapse control in `frontend/src/components/PreviewPane.tsx`.
- Added runtime/model health visibility to `frontend/src/components/CockpitHeader.tsx`.
- Updated `frontend/src/styles/index.css` for resize handles, rails, and warning status pills.
- Added `docs/architecture/agent-ide-redesign.md` with lessons from Claude Code, Codex, opencode, Cursor, and coding-agent failure research.

Verification:
- `npm.cmd run build` in `frontend` passed.
- `npm.cmd run build` in `electron` passed.

Important conclusion:
- Local model startup is real according to GUI logs. The remaining issue is reliable agent behavior: structured workflow, constrained action output, tool-call repair, and better UI state.

## API-first LLM runtime refactor

Delivered:
- Switched runtime direction to API-first LLM providers, keeping local GGUF as explicit experimental fallback.
- Added provider presets for DeepSeek, OpenAI, OpenRouter, DashScope/Qwen, Zhipu GLM, Moonshot/Kimi, SiliconFlow, MiniMax, Claude, and Local GGUF.
- Added real GUI configuration for provider/model/base URL/API key. The frontend now sends these values when creating sessions.
- Added server-side runtime LLM switching through `TapMakerAgent.set_llm()`, updating the agent, ReAct loop, memory manager, reflection engine, and skill generator.
- Added per-provider API key storage and prevented stale keys from leaking across providers when switching.
- Replaced optional SDK/requests dependency in API clients with Python stdlib HTTP calls.
- Added `UnconfiguredLLM`, which lets the GUI boot without a key but never fabricates model output.
- Fixed corrupted React components around provider selection, chat messages, and chat panel.
- Fixed the Vite Windows build issue by setting `root`, relative `outDir`, and explicit Rollup input.
- Documented the decision in `docs/architecture/api-first-llm-runtime.md`.

Verification:
- `npm.cmd run build` in `frontend` passed.
- Bundled Python `py_compile` passed for the changed LLM/server modules.
- Lightweight provider switching test confirmed a DeepSeek key is not reused for OpenRouter when no OpenRouter key is set.

Next:
- Launch the GUI and test one real API task with the user's chosen provider/key.
- Replace the raw ReAct transcript with a structured Agent Workbench flow.

## Provider model discovery follow-up

Delivered:
- Added `POST /llm/models` so the GUI can fetch model lists automatically instead of requiring manual model id typing.
- OpenAI-compatible and Anthropic providers try live `/models` when an API key is available.
- Providers without a reliable online list, including MiniMax, use built-in model hints.
- `ProviderSelector` now shows a model dropdown, refresh button, and a custom model id input.

Verification:
- `npm.cmd run build` in `frontend` passed.
- Bundled Python `py_compile` passed for model discovery backend changes.
- Smoke test confirmed MiniMax returns `MiniMax-M1` / `MiniMax-Text-01` candidates and no-key DeepSeek falls back to preset candidates.

## Desktop layout correction follow-up

Delivered:
- Clamped persisted Agent sidebar width to 300-420px so old localStorage values can no longer expand the left pane to half the window.
- Reduced IDE side widths: file tree defaults to 200px, preview defaults to 340px, editor keeps at least 520px where possible.
- Rewrote `PreviewPane` and `BrowserPreview` with clean Chinese copy and no default self-preview of `127.0.0.1:7345`.
- Browser preview now shows a blank useful state until the user explicitly loads a URL.
- Tightened header, provider settings, token meter, chat empty state, and IDE panel padding.

Verification:
- `npm.cmd run build` in `frontend` passed.
- Playwright visual check was not completed because the local node REPL asset path failed; rely on build plus next manual GUI refresh.

## Agent conversation and three-layer workbench follow-up

Delivered:
- Reframed the left panel around the three core layers: Agent layer, core runtime layer, and learning transformation layer.
- Rewrote `ChatPanel` and `ChatMessage` with clean Chinese copy and a more natural agent conversation structure.
- Restored visible LLM thought events as `Agent 正在思考` cards.
- Removed duplicate output rendering: final model output now appears as the Agent reply, not both an output event and an assistant message.
- Reduced event noise: successful observations no longer spam the chat; failures still show structured runtime feedback.

Verification:
- `npm.cmd run build` in `frontend` passed.

## Layout structure correction follow-up

Delivered:
- Changed the desktop shell from "Agent-left, IDE-right" to "IDE main workspace, Agent right copilot".
- Rewrote `frontend/src/App.tsx` to remove garbled text, put `IdeLayout` first, and move `ChatPanel` into a right sidebar.
- Reversed Agent resize behavior for the right sidebar and clamped it to 300-380px.
- Collapsed provider/model configuration behind a compact model summary button so it no longer dominates the first viewport.
- Rewrote `ProviderSelector` and `ChatInput` with clean Chinese copy.

Verification:
- `npm.cmd run build` in `frontend` passed.

## TapTap Maker layout and shutdown follow-up

Delivered:
- Re-aligned shell layout with TapTap Maker web: left task/agent panel, right main workspace/stage.
- Kept provider/model config collapsed by default so the left panel behaves like a task console rather than a settings page.
- Restored left-panel resize behavior and kept width in the TapTap-like 320-420px range.
- Updated Electron shutdown so closing all GUI windows stops the Python backend process tree with `taskkill /T /F` on Windows.
- Added cleanup for any backend listener on port 7345, so reused App Server processes are also closed when the GUI exits.

Verification:
- `npm.cmd run build` in `frontend` passed.
- `npm.cmd run build` in `electron` passed.

## Stage-first workspace correction

Delivered:
- Changed `IdeLayout` from file/editor/preview equal columns to file/sidebar + central Preview stage + right Code drawer.
- When no file is open, the code drawer collapses to a narrow rail so empty editor space no longer dominates the workspace.
- The Preview stage is now the main workspace surface, matching TapTap Maker's stage-first layout better.

Verification:
- `npm.cmd run build` in `frontend` passed.

## Full runtime review, performance pass, and roadmap

Delivered:
- Reviewed the current backend/Agent/LLM/frontend architecture around runtime isolation, SSE semantics, provider switching, session persistence, and GUI recoverability.
- Restored explicit `mock` support in `LLMFactory` so local tests and injected test runtimes remain usable without real provider keys.
- Serialized `AppServer.run_session()` with `_run_lock` to prevent the shared `TapMakerAgent` from cross-contaminating LLM provider, event sink, and approval callbacks across concurrent sessions.
- Changed session LLM reconfiguration so it only happens when a session explicitly sends provider/model/base URL/API key overrides; injected runtimes are preserved otherwise.
- Fixed unconfigured provider startup failures so sessions persist as `error` and emit a terminal `status.done` event instead of hanging in `running`.
- Changed frontend SSE error handling so recoverable ReAct `error` events stay in the timeline and only fatal/done errors close the stream.
- Hardened `SessionStore` with SQLite foreign keys, busy timeout, WAL, and normal synchronous mode.
- Hardened static file boundary checks and made SSE/client disconnects quiet in server logs.
- Added `docs/architecture/code-review-roadmap-2026-06-22.md` and `docs/architecture/adr-0001-serialize-shared-agent-runtime.md`.
- Added a regression test for unconfigured provider sessions marking error/done.

Verification:
- Bundled Python `py_compile` passed for changed server/session/LLM/test modules.
- `npm.cmd --prefix frontend run build` passed.
- `tests/test_gui_flow.py` passed as a script.
- Direct function-run coverage passed for `tests/test_session_store.py`.
- Direct function-run coverage passed for `tests/test_app_server_resume.py`, including the new unconfigured-provider regression.

Next:
- Extract a per-session `AgentRuntime` to replace the temporary shared-agent serialization lock.
- Build `AgentWorkbenchState` normalization in the frontend so raw ReAct events become plan/current-step/tool/artifact/verification views.
- Add constrained action JSON and tool subset ranking to reduce prompt weight and action parse failures.

## Session-scoped runtime continuation

Delivered:
- Replaced the temporary shared-Agent `_run_lock` mitigation with session-scoped `TapMakerAgent` instances created per task session.
- Added `Config.clone()` so provider/model/API key overrides mutate only the session config, not the control-plane config.
- Added `MCPIntegration.attach()` so session agents reuse the base AppServer agent's shared Maker MCP client instead of launching a new `npx @taptap/maker` process per session.
- Added optional `connect_mcp` and `shared_mcp_integration` parameters to `TapMakerAgent`; session agents own ReAct/Executor/LLM/callback state but do not own the shared MCP lifecycle.
- Updated `server/app_server.py` so `run_session()` builds a session agent, wires SSE/approval callbacks onto that agent only, runs it, closes it, and preserves terminal session persistence.
- Added a regression test proving concurrent slow sessions no longer emit the old "queued" status.
- Superseded ADR-0001 and added `docs/architecture/adr-0002-session-scoped-agent-runtime.md`.

Verification:
- Bundled Python `py_compile` passed for `core/config.py`, `agent/mcp_integration.py`, `agent/agent.py`, `server/app_server.py`, and updated tests.
- `tests/test_gui_flow.py` passed.
- Direct function-run coverage passed for `tests/test_app_server_resume.py`, including the new concurrent-session regression.
- `tests/test_mcp_client.py` passed.
- `tests/test_app_server.py` passed.
- `tests/test_session_store.py` direct function run passed.
- `npm.cmd --prefix frontend run build` passed.

Next:
- Build frontend `AgentWorkbenchState` normalization so session events become a stable workbench model rather than raw chat cards.

## AgentWorkbenchState frontend continuation

Delivered:
- Added `AgentWorkbenchState` and `WorkbenchToolRun` types to `frontend/src/hooks/useBackend.ts`.
- `useBackend` now maintains structured session state from SSE events: stage, current status, current thought, current tool, tool runs, last error, final output, and iteration.
- Added `frontend/src/components/AgentWorkbench.tsx` as a compact run-state panel.
- Wired `AgentWorkbench` into `ChatPanel` above the raw event timeline.
- Added CSS for stage badges, current tool, thought preview, recent tool runs, and error state.

Verification:
- `npm.cmd --prefix frontend run build` passed.
- `tests/test_gui_flow.py` passed after the frontend changes.

Next:
- Move to constrained action JSON and tool subset ranking in the ReAct/LLM action path.

## Maker cockpit usability and runtime continuation

Delivered:
- Default preview now opens `https://maker.taptap.cn/` in browser mode.
- Chat input stays enabled during a run; extra messages are queued and executed after the current session.
- The lower-left add-file button now calls Electron `dialog.showOpenDialog`, with a web file-input fallback.
- File/assets collapsed rail now shows explicit `文档` and `素材` buttons instead of a thin vertical label.
- Chat, file tree, and asset panels have stable overflow and visible scrollbars.
- Static three-layer UI copy was replaced with real `layer` events and `AgentWorkbenchState.layers`.
- ReAct now ranks and caps candidate tools per iteration before think/action prompts.
- API action prompts now use constrained JSON and a short repair fallback.
- ReAct emits `tool_selection` events so the UI shows which tools are being considered.
- Roadmap updated in `docs/architecture/code-review-roadmap-2026-06-22.md`.

Verification:
- `npm.cmd --prefix frontend run build` passed.
- `npm.cmd --prefix electron run build` passed.
- Bundled Python `py_compile` passed for changed backend/LLM modules.
- `tests/test_gui_flow.py` passed.
- Direct function-run coverage passed for `tests/test_tool_call_validation.py`, `tests/test_app_server_resume.py`, and `tests/test_mcp_client.py`.

Next:
- Extract lean `SessionRunner`/`AgentRuntime`.
- Promote Maker MCP to a first-class tool group with health/version/tool list/last-call/failure diagnostics.
- Add latency metrics for first response, task gaps, tool calls, Maker MCP calls, and browser actions.
- Add progressive loading and virtualized logs/assets/file tree.
- Build a skill compatibility bridge for Claude Code, Codex, OpenCode, and TTMEvolve canonical skills.

## Agent transcript display correction

Delivered:
- Reworked Agent chat/event styling from clipped card-like blocks into a continuous transcript.
- Removed hidden overflow, shadowed card containers, fixed-card visual treatment, and full-block event backgrounds from message/event rows.
- Tool calls and observations now use full-height `pre-wrap` text with no max-height clipping.
- Event type distinction now uses a slim left rail and header color, not boxed cards.
- Removed internal `status` and `tool_selection` events from the main chat transcript; those now update Workbench state only.
- Changed collapsed rails into narrow controls: left chat shows `AI`, file/assets rail shows `文`/`素`, and code drawer shows `{}`.

Verification:
- `npm.cmd --prefix frontend run build` passed.
- `tests/test_gui_flow.py` passed.

Next:
- If event logs become very long, virtualize at the list level while keeping each visible event full-content and uncut.
- Keep internal debug data out of the user-facing transcript unless the user explicitly opens a debug view.

## Preview stage browser correction

Delivered:
- Simplified the central preview stage into a browser-only Maker surface.
- Removed the file/browser mode switch and file/media preview branches from `PreviewPane`.
- Removed selector, JavaScript, logs, and browser meta controls from the main preview area.
- Added `/browser/click_at` and `BrowserService.click_at()` so clicking the screenshot forwards page coordinates to Playwright.
- Set the Playwright viewport to a stable 1365x768 and maps screenshot clicks to that viewport.
- Reduced screenshot polling from every 2 seconds to every 5 seconds and stopped fetching console logs in the preview loop.
- Added a service-level `test_click_at` regression.

Verification:
- `npm.cmd --prefix frontend run build` passed.
- `npm.cmd --prefix electron run build` passed before final browser-only change; frontend and Python compile passed after it.
- Bundled Python `py_compile` passed for `server/browser_service.py`, `server/app_server.py`, and `tests/test_browser_service.py`.
- `tests/test_gui_flow.py` passed.

Next:
- Replace screenshot-backed browser preview with Electron `BrowserView`/`WebView` or a CDP-backed live view for true DOM-level interaction and better performance.
- Move file preview behavior into the file/editor side instead of the central Maker preview stage.

## Native BrowserView preview continuation

Delivered:
- Added Electron `BrowserView` support for the central Maker preview stage.
- Added preload bridge `electronAPI.makerBrowser` with `show`, `hide`, `setBounds`, `navigate`, and `reload`.
- React `BrowserPreview` now prefers native `BrowserView` in Electron and only falls back to screenshot-backed Playwright preview outside Electron.
- The native browser is positioned by syncing the React host element's window bounds to the BrowserView.
- Removed Electron GPU-disable switches so Chromium preview can use normal rendering acceleration.
- Kept Playwright browser service for Agent tools and fallback mode; it is no longer the primary user-facing preview in Electron.

Verification:
- `npm.cmd --prefix frontend run build` passed.
- `npm.cmd --prefix electron run build` passed.
- `tests/test_gui_flow.py` passed.

Next:
- Add runtime smoke verification by launching Electron and checking BrowserView bounds/interaction in the actual window.
- Later, decide whether to migrate from deprecated `BrowserView` to Electron `WebContentsView` if/when the project moves to a newer Electron API surface.
